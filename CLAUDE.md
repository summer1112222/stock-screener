# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **README.md 已更新**（含路由速查/架构/合规/数据源限制，面向用户）。**开发仍以本文件的"路由速查"和"架构"为准**——CLAUDE.md 含非显而易见的设计决策与改动检查清单，README 面向使用者。

## 合规硬约束（最高优先级）

本项目是**数据筛选/回测研究工具，非投资咨询**。任何改动都必须守住：

- **不荐股、不输出实时买卖点、不承诺收益（含"月 10-20%"这类表述）、不自动下单**。无证券投资咨询资质。
- 所有数据响应经 `api/server.py` 的 `_wrap()` 附 `disclaimer` + `update_time`；回测响应附 `bt_disclaimer`；候选池附 `cand_disclaimer`。
- "候选池"是按因子**机械排序的观察清单**，不是"推荐买入"。措辞必须用"筛选/排序/观察清单"，不得用"推荐/买入/卖出"。
- `config.yaml` 注释、每个模块 docstring 都重申此边界——改它们时保持一致。

## 常用命令

以下命令均应从仓库根目录运行；项目没有 `pyproject.toml`、`pytest.ini` 或独立 lint 配置。

```bash
# 安装运行/测试依赖（Python 3.12）
python -m pip install -r requirements.txt
python -m pip install -q pytest

# 本地运行(宿主需装 pandas；akshare 仅采集层需要)
uvicorn api.server:app --reload --port 8000

# Docker（推荐；Dockerfile 配了清华 PyPI 镜像，否则国内 pip 下载仅 17kB/s）
#   部署优先用 deploy.sh(跑测试→重建→健康检查→模块自检,抓镜像漂移)而非手动 docker cp:
#   bash deploy.sh              # 测试+重建+启动+健康检查
#   bash deploy.sh --skip-test  # 跳过测试(快)
#   bash deploy.sh --no-build   # 不重建镜像,仅重启
docker compose up --build -d            # 重建并后台启动(deploy.sh 内部即此+校验)
docker compose down                     # 停止(保留数据卷)
docker compose down -v                  # 连同 SQLite 卷一起清
docker compose logs -f screener         # 查看服务日志

# 单测（宿主跑；tests/ 被 .dockerignore 排除，镜像里没有）
# 环境：Python 3.12.10 + pandas 3.0.3 + requests 2.34.2（见 requirements.txt）
# 无 conftest.py / pytest.ini / pyproject.toml——必须在仓库根目录跑，子目录里跑找不到 data 包
python -m pip install -q pytest         # 宿主若无 pytest
python -m pytest tests/ -q
python -m pytest tests/test_engine.py::test_topn_asc -q   # 单个测试
python -m pytest tests/test_factor_research.py -q       # 因子研究台
python -m pytest tests/test_execution.py tests/test_engine_execution.py tests/test_signals_execution.py -q  # 成交模拟与 T+1 回测

# 快速静态检查（仓库未配置独立 lint 工具）
python -m compileall api data screener backtest scripts tests

# 龙虎榜历史回填（按需执行，幂等）
python -m scripts.backfill_lhb_history 6 30

# 触发数据采集（GET 与 POST 均可，浏览器地址栏可直接访问）
curl -X POST http://localhost:8000/api/refresh

# 前端：http://localhost:8000/web/index.html  （FastAPI 同源托管 /web + /api）
```

## 架构

四层 + 两个领域模块：

- `scripts/` 一次性工具脚本（不进采集流程）。`backfill_lhb_history.py`（龙虎榜历史回填：`python -m scripts.backfill_lhb_history [months=6] [batch_days=30]`，幂等可重复跑）、`diagnose_ranking.py`（三清单排序诊断：`python -m scripts.diagnose_ranking [module]`，**只读** `list_track` 前视收益评估排序有效性——输出头部 vs 整体(top10/20 vs 全体 k1/k3/k5 中位数/胜率)/五因子分档(nextday 解 meta_json.factor_scores 按值 5 档看 ret_k3 单调性)/穿透分层(quality_pct/sector_heat 高低两半区分度)/quality 通过率结构，**不触网不改排序**，仅在 `list_track` 已积累样本后跑才有意义）、`fill_track_returns.py`（清单前视收益回填脚本，幂等可重跑，对应 `/api/track/fill`，只读 `stock_daily` 现有历史不自动拉数据）、`quality_diag.py`（quality 口径有效性一次性诊断：口径1 构成因子在 stock_daily 全历史滚动分档单调性+Rank IC（复用 backtest.eval）+当前 main 清单审计（confidence/risk_flags/口径覆盖），读库不写库）、`etf_source_probe.py`（Phase0 ETF 数据源出口 IP 可用性探针，结果写 `data/cache/etf_source_probe.json`，只读不触生产逻辑）。
- `data/` 采集与存储。`models.py`（规范字段集、AKShare 别名映射、SQLite schema）、`db.py`（连接/建表/迁移/upsert/query/meta；**线程本地连接复用(2026-09-21)**：`get_conn` 用 `threading.local` 每线程复用一条长活连接替代每操作 connect+PRAGMA×4，实测每查询 3.33ms→0.13ms(96%节省)；`__exit__` rollback 未提交事务，保持"须显式 commit 否则丢弃"旧语义；连接缓存带 `DB_PATH` 校验——测试按 per-test tmp 动态切库或连接损坏时惰性重建，生产 DB_PATH 恒定仅每次比较字符串零开销；新补 `idx_list_track_module_date`(list_track(module,mode,date))）、`collector.py`（spot 快照采集：板块/ETF）、`history.py`（历史日线采集）、`portfolio.py`（本地持仓表：记录买入、按 spot 最新价算浮盈）、`watchlist.py`（自选股观察清单：未买入跟踪，按 spot 现价显示，同 code 加重覆更新不重复；表 `watchlist(id,code,name,note,added_ts)` 镜像 portfolio，用独立 INSERT 不走 `upsert_rows`/`TABLE_FIELDS`）、`fundamentals.py`（完整三大财报按需+缓存，7天TTL，超时降级 stale；**tdx 主源**：`parse_tdx_financial(code)` 调 `pytdx_client.get_company_info(code,"财务分析")` 取文本→解析为 {abstract,balance,cashflow,profit}，`fetch(code,source)` tdx 主源+akshare `stock_*_sheet_by_report_em` 降备援；`_cache_get/_cache_set` **必须 `_strip_prefix`**（与 `_fetch_net` 对称，否则 fetch_abstract 预填带前缀键与 fetch 查询键不匹配）；缺源写空 df 哨兵使命中返 None 秒回不重 parse）、`research.py`（研报评级+千股千评）、`smart_money.py`（主力动向采集：龙虎榜席位/十大流通股东/陆股通/个股资金流/高管增持仓/限售解禁 6 通道，每通道 `(records,ok,err)`，东财优先+备援(龙虎榜走 finshare `get_lhb`,不走东财 push2 被封端点)+失败标不可用不崩；`refresh_today` 编排(6 通道 `ThreadPool` 并行采集+串行 `upsert` 避 SQLite 并发写锁;THS 资金流 token 模块级缓存 TTL10min+分页 8 并发 ~70s→5-8s;高管 17 万行 7 日 pickle 缓存 `_cache_dir` 优先 env/卷`/app/var`,命中 4-5min→0)+ `CHANNEL_STATUS`/`NATIONAL_TEAM`)+ `backfill_lhb_recent`(龙虎榜 T+1 滞后自动补采:max(date) 落后于最近交易日则逐交易日补采到最近,幂等无榜日空,启动/refresh 后 server 后台 daemon 触发)、`market.py`（市场温度采集：涨跌家数比纯 DB+legu 估值+两融余额+估值，4 路降级，入 `market_daily` 表 upsert；`latest()`/`trend(days)`）、`board_stocks.py`（板块成分股采集：东财 `stock_board_industry_cons_em` 出口 IP 被 RemoteDisconnected 封死无备援 → 改走同花顺 thshy/gn 详情页静态表（每页 20 可翻页，read_html 解析，代码列 read_html 丢前导零需 `zfill(6)` 补回），按需实时拉取不入库，7 天 TTL 内存缓存 name→code 映射；`_prefix` 给 6 位代码加 sh/sz/bj 交易所前缀）、`pytdx_client.py`（通达信行情直连薄客户端：免 key TCP 7709，服务器池单例复用+心跳重连+`threading.Lock`；`get_quote` 批量五档实时/`get_daily_bars` 日K(raw 不复权,800 偏移分页)/`get_xdxr` 除权除息因子(供本地算 qfq)/`get_company_info` 公司信息文本(16 类，**"财务分析"类经 `fundamentals.parse_tdx_financial` 解析为摘要+三大表**，财报主源；其余类返原文本不解析)/`get_block_members(category)` TDX 板块成分股文件(industry/concept/all，nextday step5 批量反查用；TDX 行业文件常缺"通信设备"等板块，缺失板块需逐个降级 `board_stocks.fetch_constituents`，勿用 any() 短路整体降级)；**历史日 K 主源**(raw+本地 qfq,见 `adjust.py`)；portfolio spot 缺价→tdx 兜底、stock_analysis 加 latest_price）、`adjust.py`（前复权本地计算：`get_xdxr(code)` 取 pytdx 除权除息记录并归一化为每股(fenhong/songzhuangu/peigu 均 ÷10)+`qfq(raw_df,xdxr_df)` 按**除权日降序累乘 ratio** 算前复权(volume 反向 ÷扩股比)；供 history tdx 主源用,使通达信成历史日 K 自洽主源不再依赖新浪/东财 qfq 接口）。
- `screener/` 实时筛选。`conditions.py`（因子目录 `BOARD_FIELDS_CAT/ETF_FIELDS_CAT`、运算符 `OPS`）、`engine.py`（快照过滤/排序/资金流合并、派生因子 `DERIVED_FIELDS`）、`indicators.py`（可复用的 pandas 技术指标纯函数，如 SMA/EMA/RSI，不负责采集或 API）、`nextday.py`（次日强势，**2026-09-06 重构**：并入 daily_strong 五步漏斗作诊断+strict 选择，排序主键改五因子连续评分[横截面 rank-pct+可用因子重归一，IC 校准权重 mom_5_1 0.30/rel_strength 0.20/sr_10 0.15/close_vol_corr 0.10/liq_turnover 0.15]，`selection_mode` strict/score/fallback；**触网**：tdx get_quote 盘口增强+缺 stock_daily 历史按需补~180日+板块成分股按需取；粗筛 top 200+30s缓存；不再用 smart_money/chip 因子；**A段板块成分股有界化(2026-09-19)**：穿透标注只取"被评分热点板块"前 `_SECTOR_CTX_K=20`(按资金净流入降序∩industry_board,`_scored_sector_names`),勿全板块扫——全量~500 行业板块逐个降级 THS `fetch_constituents` 曾 531 次累计 316s > 前端 120s AbortError;`_board_members_batch` 降级路径加 `_BOARD_BATCH_CACHE` 30s TTL(同板块复用,`_fetch_board_constituents_cached`);`get_finance_info` 只覆盖前 `_TDX_ENRICH_K` 候选(勿全候选逐股串行 ~22s);冷缓存实测 126.4s→45.4s(<120s 首轮超时)。同步 tests(3 新测试)）、`daily_strong.py`（每日强势5步混合漏斗+板块助攻/硬剔除+软打分/30s缓存；前端 dsLoad/dsRender 已退役，模块保留仅供 `/api/daily-strong` 兼容转调）、`smart_money.py`（主力动向查询/聚合层，只读 `db.query_rows`：`today_list`(limit 默认 1000 截 top N,0=无限制;命中默认参数时经 tracker 落 `list_track`(mode=today, 头部 top20, score=净额, meta=quality_pct),见 tracker/检查清单)/`by_actor`(actor LIKE 下推 SQL,含"国家队"保留词 OR 多名展开)/`top_by_amount`(附 `_attach_intensity` net_intensity=主力净额/当日成交额 强度归一化,操盘手跨市值可比维度,只读 `stock_spot`);另含 `chip_distribution`(P1 筹码分布，移动成交量加权成本，**不入库不触网**进程内按需算，复用 `backtest.signals._uni_panels` 取 `stock_daily` close+amount)/`behavior_series`(P2 主力行为序列，读 `smart_money_action` 按 channel 聚合算连续净流入/流出天数·累计净额·边际加速·daily 序列;资金流通道表无记录时按需 `finshare.get_money_flow_stock` 补历史主力净额序列(标 source=finshare,不走东财被封端点),**补到的序列落库缓存**(upsert `smart_money_action`,下次同 code 直接 DB 读避免重拉,属按需单股写非 refresh 全量,与"不进 refresh 避免 5200 次调用"一致));另含 `main_force_phase`(主力阶段计分制4阶段[吸筹/洗盘/拉升/出货]+观望,confidence=命中条件数/总条件数,并列按风险优先级 出货>拉升>洗盘>吸筹 保守取胜,复用 behavior_series+chip_distribution+stock_spot+近60日高位分位,30s进程缓存)/`board_money_link`(板块-个股资金联动:所属板块净流入排名+百分位+板块内成分股 net_intensity 横截位置+板块5日趋势)/`seat_winrate`(席位胜率:finshare 回填龙虎榜历史+stock_daily 前视 k 日收益中位数/胜率,多 k 5/10/20,国家队展开,依赖 `scripts/backfill_lhb_history.py` 一次性回填)/`chip_distribution` 加 `trend`(筹码迁移:近5/20日获利盘·集中度·成本移动Δ,>0 套牢盘消化偏多/<0 筹码聚拢主力收集)/`radar`(多通道主力共振雷达,只读 `smart_money_action` 按 code 聚合各通道 daily+streak+边际加速,量纲分开:股数通道[十大股东/高管/限售]与金额通道[资金流/龙虎榜/北向]绝不混比)。、`sector_heat.py`（行业景气排序加成层，纯函数+静态 `POLICY_THEMES`(2026-09 六大主线先进制造/电子信息/智能家居消费/基础研究算力/人形机器人/智能驾驶,人工维护;**2026-09-20 板块名校准对齐 `industry_board` 真实行业板块名**——原混入风格/概念名[先进制造/智能驾驶/算力/CPO/人形机器人/减速器/传感器] industry_board 无,`_board_members_batch("行业")` 反查不到个股致 policy_hit 恒 0,改用真实行业名[机器人/半导体/通信设备/汽车零部件…];加 `unmatched_themes(available_boards)` 诊断函数,喂数据源真实板块名集合即报各主题缺失待对齐项,纯函数不触网):`board_heat`=0.6*板块资金净流入 rank-pct+0.4*板块上涨宽度 rank-pct(up/(up+down))，`policy_hit` 静态政策命中+0.05，`attach_sector_heat` 原地给行附 `sector_heat`/`policy_hit`/`style_hit`/`policy_event`(政策事件标注,复用最优板块);**B2 宏观风格调制(2026-09-20)**：`attach_sector_heat` 加可选 `style` 参数(macro_style 返回 dict)，给定时 policy_hit 按宏风格动态调制——`board_beta(board)` 判板块风格方向(offensive=进攻主线高贝塔[从 `_OFFENSIVE_THEMES`=先进制造/电子信息/算力/机器人/智驾 提取]/defensive=防御红利[`_DEFENSIVE_BOARDS` 手动维护 银行证券保险公用事业电力食品饮料白酒煤炭高速港口 等]/neutral)，`policy_addon(board,style)` 返 `(policy_hit, style_hit)`：政策命中进攻主线板块 ×`_STYLE_MULT`(attack 1.6/defense 0.6/均衡 1.0)，风格方向加成 `_STYLE_ADDON=0.03` 仅"进攻主线 in attack / 防御板块 in defense"生效；`style` 缺省→退化为静态 `policy_hit` 0.05 + `style_hit=0`，兼容旧调用与测试；quality 精排 `_refine_tail_body` 经 `data.market.latest()`→`macro_style` 取 style 传入，`_final` 计入 `style_hit`(见 quality)；）；个股归板块经 `nextday._board_members_batch` 反查(stock_spot 无 board 列),仅传"被评分板块"(sector_fund_flow行业今日∩industry_board 名)有界单次调用;不触网不新增表不新增采集;被 quality 精排与 smart_money 清单用作**排序上下文/标注**,不改共振签名)、`policy_events.py`（事件型催化标注层,纯函数+静态 `POLICY_EVENTS` 人工事件表[事件→相关行业板块名,板块名对齐 industry_board 真实行业名,含 date/title/note,end 可选过期日]:`event_hit(board,as_of)` 返回该板块命中政策事件列表[id/date/title/note,**只标注不进排序键**],`attach_policy_events(rows,member_map,as_of)` 原地附 `policy_event`(多板块命中聚合去重,无→[]);as_of 过滤过期事件;不触网不新增表,被 `sector_heat.attach_sector_heat` 顺带复用;措辞"事件催化机械标注非买卖信号"),`macro_style.py`（全市场宏观风格基调状态机，**B1 地基层，2026-09-20**：把 `market_daily` 最新行（由调用方 `market.latest()` 传入，本模块不查库不触网）捏成 `score∈[-1,1]`+`style∈{attack/neutral/defense}`+`strength=|score|`；四因子加权 `_W={zt:0.30, margin:0.25, pe:0.25, breadth:0.20}` 经验先验非 IC 校准，映射 涨停家数(绝对阈值 `_ZT_REF=50` 饱和)、两融环比(tanh/`_MARGIN_REF=50亿`)、pe_pct(`(0.5-v)*2`，低分位→进攻有空间)、涨跌家数宽((ratio-0.5)*2)；缺因子按可用加权重归一（同 nextday `_weighted_score`），缺失从分母排除，全缺失诚实 `neutral/strength 0`，不除零不崩；风格阈值 `_STYLE_EDGE=0.2`；返 `drivers` 逐因子 raw/contrib/weight 供调试。**约定了 B2/B3 消费口**：B2 用 style 调制 `sector_heat` 板块政策命中加成（进攻放大高贝塔主线/防御放大防御板块），B3 用 style×事件时效调制 `policy_events` 催化权重——本层自身不改任何排序签名。措辞"市场风格机械观察非择时信号"，挂 `cand_disclaimer`。同步 `tests/test_macro_style.py`(11 测试)）,`etf_screen.py`（ETF/QDII 长短清单编排：`etf_screen_rank(universe=ETF/QDII, mode=long/short, codes, limit, days)` → 键 `long_term`/`short_term`/`mode`；**数据诚实抽象** `_fetch_qdii_premium`/`_fetch_quality_meta`/`_fetch_index_valuation` 按需 akshare+模块级缓存,失败/无 IOPV 诚实 None(**不读 `etf_spot` 不存在的 nav/fund_scale 列**——实测表仅 code/name/latest_price/change_pct/turnover_amount/turnover_rate;QDII 溢价=(最新价-IOPV)/IOPV 主源 `fund_etf_spot_em` 全市场单次拉);long=质量(规模硬门槛 `_MIN_SCALE`10亿/费率/跟踪误差)+估值分位+QDII 高溢价降权标 risk_flag;short=量价横截面 rank(动量/波动/量能,复用 `backtest.eval.load_panel`);`_display_name` 本地占位 name==code 时回退 `fund_etf_spot_em` 名称/基金简称取真实名,无源诚实返回占位前端显"待刷新";30s 进程缓存 `_CACHE`(键含 universe/mode/codes/limit/days);估值/质量/QDII meta 全 mock 可测不触网;同步 `tests/test_etf_screen.py`)。
- `backtest/` 历史研究。`eval.py`（IC/IR/分档/因子计算）、`engine.py`（topN 回测，注意与 `screener/engine.py` 同名不同域）、`risk.py`（风控指标）、`robust.py`（walk-forward/bootstrap）、`candidates.py`（候选池排序）、`execution.py`（统一次日开盘成交模拟器：`ExecutionConfig`(滑点 slippage_bps/佣金 commission_bps/印花税 stamp_tax_bps/过户费 transfer_fee_bps)+`execute_entry`/`execute_exit` 按 `execution_mode` 取价(close=当日收盘/next_open=次日开盘)+涨跌停封板判定，被 `signals.backtest_signals` 与 `engine.run_topn` 经 `execution_mode` 参数复用，仅历史研究不构成实时交易建议）、`research.py`（因子有效性研究：多前瞻周期 IC/Rank IC、分层收益、训练/测试切分、成本后结果与可选 walk-forward；严格使用 t 日及以前数据，只做历史统计，不改筛选排序）、`buffett.py`（巴菲特式基本面分析 v2：年报 EPS + 横截相对估值分位 + FCF 代理 + 杠杆校正 ROE + 负面预筛，**仅个股**，ETF 不适用；FCF 优先用 `fundamentals_cache` source=cashflow 算真实 FCF（经营现金流-资本开支），失败降级摘要代理（经营现金流量净额），`ratios.fcf_source` 字段标注来源；附带修复 `_annual` 兼容带破折号报告期 2024-12-31）、`tracker.py`（清单历史追踪层：把 quality/nextday/smart_money 每日机械清单落库 `list_track` 表，幂等回填 T+1 open 买入/T+1+k close 卖出前视收益，并按市场温度/行业强弱汇总；`DEFAULT_PARAMS` 已含 `smart_money`(date/channel/market/days=7/limit=1000)，`record_list` 白名单={quality,nextday,smart_money}，`_meta_payload` 对 smart_money 只摘 quality_pct 落 meta_json，score=净额；`is_default_params(module,params)` 仅默认参数组合才记录样本防任意参数污染统计，被 server 的 `/api/quality`、`/api/nextday-strong`、`/api/smart-money/today`(仅头部 top20,不改变响应) 转调记录，`/api/track/fill` 触发回填）、`signals.py`（买卖信号扫描：ma_breakout/golden_cross/volume_surge/rsi_oversold/momentum_up，依赖 `*_daily` 历史表，复用 `data.history._UNIVERSE`；`scan_signals` 支持 `min_hits` 共振过滤(每行带 `hits`/`combo`)，`backtest_signals` 支持 `min_hits`(≥2 追加 combo≥N 行)/`stop_loss`(前视 k 日 low 截断)/`fee_bps`(双边费率)；`_uni_panels` 加 `with_ohlc` 取 high/low，默认 2-tuple 回退兼容旧测试 mock）、`quality.py`（多口径共振编排层：四口径分位[风险调整/价值质量/资金流向/多信号]→共振分 hits×10+avg→min_dims 门槛→组合层[行业分散/相关性/容量]；**只读** buffett/signals/smart_money/candidates 结果，不新增表，个股/ETF 不混跑，ETF 口径2恒空；盘口精排阶段(2026-08-13)：四口径共振粗筛 top50 → pytdx get_quote 取实时盘口 → A 流动性深度重排(0.6共振+0.4流动性横截分位) + B 挂单不对称/C 内外盘比 仅 raw 展示不进排序(合规:方向=择时非质量),盘后自动降级(A/B失效仅留C全天内外盘),refine/refine_pool 参数,盘中缓存30s;ETF 与 refine=False 跳过。措辞"盘口供求机械观察",disclaimer 追加 A/B 收盘失效。**buffett 超时优化(2026-08-14)**：`analyze_many(codes, deadline_s=None)` 默认 None 走旧 `ex.map` 等全部完成；quality 口径2 传 `deadline_s=40.0`——akshare 财报接口慢(每只~15-20s 逼近 `_AK_TIMEOUT=20`)，80只 shortlist × 15-20s / 8 worker ≈ 150-200s，旧 `ex.map` 干等导致 /api/quality 首次 cache-miss 挂起~86s + 前端裸 fetch 无超时报"加载失败"。改 `as_completed` + 总截止返**已完成部分分位**(放弃未完成，`shutdown(wait=False, cancel_futures=True)` 不阻塞等残余线程≤20s自亡)，quality 凭口径1/3/4+部分口径2 仍产有效主清单；首次 86s→~50s(<前端75s AbortController 超时，用户首次即见结果)，5min 内重算走结果缓存秒回、5min-7天走 fundamentals_cache 7天缓存秒回。改 deadline 阈值同步 `tests/test_buffett_value.py` 的 `test_analyze_many_deadline_returns_partial`）。**熔断+预热(2026-08-15)**：① `buffett._note_fetch(ok)`/`akshare_blocked()` 运行时熔断——`fetch_abstract` 所有结果(成功/超时/异常/**空结果**)经 `_note_fetch`,**空结果亦计失败**(akshare 被封常快速返空 DataFrame 非 20s 超时,旧实现空结果走 fallthrough 不调 `_note_fetch` 致熔断永不触发);连续≥3失败→`_BLOCKED_UNTIL` 熔断30min,quality 口径2 检 `akshare_blocked()` 跳 `analyze_many` 省40s白烧→下方 spot估值代理接管。akshare 慢但成功时熔断休眠(fetch成功归零),仅被封(返空/错)时触发。改 `fetch_abstract` 同步 tests(空结果/成功重置2测试)。② `server.py` `_startup_warm`/`_warm_quality_cache_background` 启动+refresh 后后台 daemon 预热(非阻塞 `_warm_lock` 防叠加):`shortlist_by_turnover(k=80)` → `analyze_many(deadline_s=120)` 填 `fundamentals_cache` 7天 + `quality_rank`(默认参数对齐前端 qLoad 下拉)填结果缓存5min;`_AK_OK=False` 跳过;失败不影响 refresh/启动。冷启动后用户开 tab 命中缓存秒回。同步 `tests/test_quality_warmup.py`(5测试)。③ 前端 `qsLoad._qsFetch` 首次75s超时**自动重试一次**(90s):容器刚重启 warmup(~55s)与首次请求争抢可能>75s,重试时 warmup 多半已完→缓存命中;二次仍败才报"加载失败"。**财报 tdx 主源化(2026-08-16)**：`buffett.fetch_abstract`/`fundamentals.fetch` 改 **tdx 主源**(`parse_tdx_financial` 解析通达信"财务分析"文本)，akshare 降备援(仅 tdx 解析失败 parsed=None 时触达，实测 tdx 返 base dict 几乎不触达)。三连性能修复根除优质筛选超时：①**缺源重 parse**→fetch_abstract/prefetch 对 tdx 缺源写空 df 哨兵，fetch 命中哨兵返 None 秒回不重 parse(pytdx 单 TCP 连接 Lock 串行，N×3 重 parse 是 deadline 40s 只出 16 的主因)；②**并发争用 parse 抖动**→`analyze_many` 前串行 `prefetch_financial(codes)` 预热填缓存(单线程无 Lock 争用)，并行阶段全缓存命中 0 次 pytdx；③**`_spot` 全表扫 stall**→改 `where code=?` 走主键索引查单行(旧无 where 扫 5200 行×8 worker 并发 GIL/内存硬 stall)。容器实测 80 只 shortlist：cold 40s/16→23.5s/72，warm 40s/0→9.5s/72；/api/quality 冷 67s(<75s 不再 AbortError) 暖 0.655s。缓存键坑：`fundamentals._cache_get/_cache_set` 必须 strip 前缀。同步 tests(test_tdx_financial 哨兵命中 + buffett_cache/timeout/fundamentals_cache 各加 parse_tdx_financial→None mock 测 akshare/stale 路径 + buffett_value deadline 测试加 prefetch_financial mock)。**因子体系升级(2026-08-15,Phase1+2)**：①`_avg_rank_pct(factors,codes)` rank-based 稳健聚合(每因子先 `_to_pct` rank-pct 再按 code 取可用因子 `mean(skipna)`,抗异常值+异质缺失)替 zscore 等权;②口径2 从 3 因子(ey/moat/lroe)扩到 6(+roic/mos/oet,全在 buffett.analyze 结果里,零新调用);③`screener/smart_money._behavior_batch(codes,days)` 批量行为序列(一次查 `smart_money_action since days 前`,按 code×channel 聚合 streak_inflow/outflow/margin_accel(近5日均-近20日均)/北向cum,复用 `_streak`,**不调 finshare 回退**——批量场景缺记录→对应字段 None),口径3 从 high-corr 三因子(top_by_amount累计+spot当日净额+换手)改为 low-corr 三因子(资金流连续性 streak_inflow+北向累计+边际加速)限 shortlist(非 shortlist 口径3=None,与口径2 一致),全空→降级旧 spot 路径(ETF/无行为序列)。改口径2/3 同步 `tests/test_quality_factors.py`(Phase1+2 共6测试)。**Phase3 共振层(2026-08-15)**：`_resonance(dim_scores,dim_thresh,weights=None,mode="greedy")` 加 mode + 默认经验权重 `_DEFAULT_DIM_WEIGHTS={1:1.0,2:1.3,3:0.6,4:0.7,5:1.3}`(因子有效性先验,非IC校准,口径2价值质量/5景气>1风险>4信号>3资金流);`mode="greedy"`(默认,hits×10+命中口径加权均值,数量偏好,共识优先)/`"penalize"`(命中口径**加权几何均值** `exp(Σwi·log(pci)/Σwi)`,pct裁剪[0.01,1]防log0,**短板惩罚**——某口径极低拉低总分;greedy 只对≥thresh 命中口径取均值,penalize 对所有 present 非None 口径取几何均值以真正惩罚短板;hits 独立返回供 min_dims 门槛);`dim_thresh` 默认 0.6→**0.7**(提区分度,主清单仍≥min_dims);`quality_rank` 加 `resonance_mode` 参数+缓存键含之;`/api/quality` 透传 `resonance_mode`(Query 默认 greedy)+ `dim_thresh` Query 默认 0.7;前端 `qsResMode` 下拉(贪心共识/短板惩罚)+ `qsThresh` 默认 0.7+ onchange 触发 qsLoad;warmup `dim_thresh=0.7,resonance_mode="greedy"` 对齐前端默认缓存键。改共振同步 `tests/test_quality_factors.py`(Phase3 共4测试:penalize惩罚/greedy不变/默认阈值0.7/默认权重)+修 `tests/test_quality.py::test_resonance_hits_formula` 显式传 `weights={}` 测纯等权公式(默认权重改非等权)。**口径5景气已实现**(研报覆盖数/评级分布/目标价);IC 透明度报告待续。**可信度+风险门槛(2026-09-05)**：三个纯函数边界——`_data_confidence`(spot/history/fundamental/flow/research 五组件加权`_CONF_WEIGHTS`→high≥.75/medium≥.50/low，不适用组件 None 从分母排除，缺失降可信度/负面触发门槛严格区分)，`_quality_gate`(财务红旗商誉>30/负债>75/fcf<0.3→hard_reject，低可信度→hard_reject，单日资金脉冲→risk_flag 不硬拒)，`_risk_penalty`(按 risk_flags 地图杠杆1.5/FCF1.5/波动2.0/脉冲1.0，上限5.0)+`_confidence_multiplier`(high1.0/medium0.85/low0.65)。`adusted_resonance=raw×multiplier−penalty`，`resonance` 兼容前端值=adjusted，`raw_resonance` 追溯。`quality_rank` 加 `strict_quality=True/min_confidence=0.50/risk_penalty=True` 并入缓存键；strict 过滤 hard_gate_pass 且 confidence≥min_confidence 才进 main，低可信度保留在 by_dim，全失败→`selection_mode="degraded"`；`_conf_inputs` 从口径分位/状态粗粒度推断输入不新增网络调用；顶层 `confidence_summary`(high/medium/low/low_excluded)。`/api/quality` 透传三参数。**注意**：财务红旗已在 `_dim_scores` 口径2 `_bad` 预筛剔除，集成层传 `fundamental=None` 不重复判定；spot 快照价格列是 `latest_price`(非 price)；`_data_confidence` 用 round 后值判 level 防 0.4999→low 边界。同步 `tests/test_quality_confidence.py`(19 测试)。**网络尾段硬墙钟超时(2026-09-19)**：盘口精排+板块反查尾段在死网络下"慢而不报错"——pytdx 逐台走完 5 服务器池(`_TIMEOUT=8`×5≈40s)+板块降级逐板块走 THS(每页 timeout=15)，只靠 try/except 捕不住挂起，冷缓存 `quality_rank` 总耗时可远超客户端 120s/150s 双 abort。改 `_run_refine_tail`：尾段(`_refine_tail_body`=盘口精排+`_apply_sector_heat` 景气加成)在 daemon 线程跑 + `q.get(timeout=_REFINE_TAIL_DEADLINE=25.0)` 硬墙钟——超时/整体异常返 None，调用方**保留未精排主清单**并诚实降级 `refine_status="skip(网络尾段超时,保留未精排清单)"`，后续 `_apply_combo`/`_enrich` 照常跑；保证 buffett(≤40s)+尾段(≤25s)+其他(≤20s)≈85s 必先于客户端超时返回。超时后 daemon 线程后台自担(pytdx `_lock` 可能被僵尸占，后续请求 acquire timeout=8 即降级不影响主请求)；与 `board_stocks._fetch_constituents_em_timed` 同范式。同步 `tests/test_quality_refine.py`(`test_quality_rank_refine_tail_timeout_degrades`：patch 0.3s 临时 deadline 快验超时分支)。**B2 宏观风格接入(2026-09-20)**：`_apply_sector_heat` 加可选 `style` 参数透传 `attach_sector_heat`，`_final` 计入 `policy_hit+style_hit`；`_refine_tail_body` 景气加成块内 `data.market.latest()`→`screener.macro_style.macro_style` 取 style 传入(取不到→None 退化为静态加成,兼容旧行为)。同步 `tests/test_sector_heat.py`(+7 测试:board_beta/policy_addon/attach 带 style/quality 精排计入 style_hit)。**主清单路径新政接入(2026-09-21)**：精排尾段 `_apply_sector_heat`(含政策/风格/事件加成)原只在 refine 成功时生效，refine=False/ETF/超预算跳过/尾段超时降级分支**完全没有政策排序**。新增 `_macro_style_ctx()`(抽出 `_refine_tail_body` 内 inline 宏观风格块复用) + `_apply_policy_bonus`(主路径重排，`_final=res_pct+policy_hit+style_hit+event_bonus`，**不含流动性**——流动性是盘口精排专属) + `_run_policy_bonus`(daemon 线程 + `_POLICY_DEADLINE=20s` 硬墙钟 + 独立在途守卫 `_POLICY_INFLIGHT_TS`，死网络挂起返 None 保留原主清单诚实省略)；`quality_rank` 在 `_apply_combo` 前对 `universe==stock and main and refine_status 不以 "ok" 开头` 补跑——refine 成功时尾段已含更强加成(含流动性)不重复。有界反查复用 `nextday._board_members_batch`(30s TTL 缓存)只传被评分板块，超时/失败保持原排序零破坏。同步 `tests/test_quality_refine.py`(+4 测试:apply_policy_bonus 排序/事件命中/refine=False 附字段/尾段超时降级附字段)。
- `api/server.py` FastAPI 单文件入口，所有路由 + `_wrap`。`web/index.html` 单页前端（原生 JS，无构建步骤，从 `/api/fields` 动态渲染字段/运算符下拉）。6 tab 分组（实时筛选/历史回测/主力动向/优质筛选/个股分析/ETF/QDII 筛选）+ 持仓右上浮窗抽屉（`#pfDrawer`，`togglePortfolio()`）+ 自选观察抽屉（`#wlDrawer`，`toggleWatchlist()`，`wlLoad/wlAdd/wlQuickAdd`）+ K线跨 tab 跳转（行点击 handler 调 `switchTab('backtest')` 再 `hkRun(c)`；任意股票代码行点击调 `switchTab('analysis')` 并填码跑 `/api/stock-analysis`）；信号扫描行与个股分析卡带"＋自选"按钮调 `wlQuickAdd(code)` 一键入自选；`.tab-panel[data-tab]` 显隐，状态保持不重新 fetch。仓库根 `research_guide.html` 是独立的研究上手指南 artifact（带 `@dsCard` 标记、自带深浅主题的静态页），**非 app 运行时一部分、未被路由托管**，改前端功能时无需同步它。

### 路由速查（server.py 全量）

`/api/meta`（最近一次 refresh 的 `update_time`，无数据时为空）`/api/health`（数据健康聚合只读：各域新鲜度三态红/黄/绿 + overall，默认 disclaimer）`/api/market`（市场温度快照+近30日趋势，只读，默认 disclaimer："机械市场状态观察，非择时信号"）`/api/fields` `/api/boards` `/api/etfs` `/api/etf-screen`（ETF/QDII 长短清单筛选：`universe=ETF/QDII`/`mode=long|short`/`limit/days/codes`，调 `screener.etf_screen.etf_screen_rank`，QDII 高溢价 long 降权、short 顶部，30s 缓存，附 `cand_disclaimer`："ETF/QDII 长短清单机械排序观察清单，非荐股非买卖信号，盈亏自负"）`/api/board-stocks`（板块详情卡+成分股列表：优先东财 `stock_board_industry_cons_em`/`concept_cons_em` 直取(板名同源最准)，东财被封/空→降级 THS 静态表+模糊匹配，两源失败→龙头股无成分股诚实标 `cons_error`，`source` 字段从首行派生，默认 disclaimer）`/api/screen`（实时筛选，附 `disclaimer`）→ `/api/refresh`（手动全量采集，`@app.api_route` GET+POST 均可）→ `/api/history`（历史日线查询）→ `/api/backtest/fetch`（拉历史进 `*_daily` 表）→ `/api/research/factor`（POST：历史因子有效性研究，多周期 IC/Rank IC、分层、训练/测试、成本与可选 walk-forward；附 `bt_disclaimer`，不改变筛选排序）`/api/backtest/eval`（IC/分档）`/api/backtest/run`（topN 回测）`/api/backtest/walkforward`（附 `bt_disclaimer`）→ `/api/candidates`（候选池排序，附 `cand_disclaimer`）→ `/api/signals`（机械信号扫描，支持 `min_hits` 共振过滤，附 `cand_disclaimer`："规则机械触发，非AI推荐，不构成投资建议，盈亏自负"）`/api/signals/backtest`（信号历史胜率回测：每信号扫历史触发点 t→t+k 收益统计；支持 `min_hits`(≥2 追加 combo 共振行)/`stop_loss`(前视止损)/`fee_bps`(双边费率)；前端"信号回测研究台"卡多 k 并行渲染 signal×k 矩阵，附 `cand_disclaimer`："历史触发统计事实，非预测"）→ `/api/portfolio` GET/POST + `DELETE /api/portfolio/{pid}`（本地持仓跟踪：GET/POST 用默认 `_wrap` 附 `disclaimer`，DELETE 按 id 平仓返回 `{closed,id}`，无额外 disclaimer）`PATCH /api/portfolio/{pid}`（到价提醒 `alert_hi`/`alert_lo`，返回 `{updated,id}`）+ `/api/watchlist` GET/POST + `PATCH /api/watchlist/{wid}` + `DELETE /api/watchlist/{wid}`（自选股观察清单：同 code 重覆更新；PATCH 修改备注/提醒字段；默认 `_wrap` disclaimer；前端抽屉 `#wlDrawer` + 信号/分析"＋自选"联动）→ `/api/watchlist/live`（自选股批量实时行情：tdx `get_quote` 盘中秒级直取，复用 `quality._is_in_session` 判盘中，tdx 空→降级 spot 陈旧快照；附 `alert_hi`/`alert_lo` 越线机械标记，附 `cand_disclaimer`）`/api/watchlist/signals`（批量信号扫描 watchlist 全体：个股/ETF 分拆 universe 调 `scan_signals`，依赖 `*_daily` 历史，附 `cand_disclaimer`）→ `/api/buffett` `/api/buffett/top`（基本面评分，附 `bt_disclaimer`："基于公开财务摘要的机械评分，护城河来源需读年报人工判断，研究优先级非买卖信号"）→ `/api/smart-money/today` `/api/smart-money/refresh` `/api/smart-money/channels`（主力动向观察清单，`today`/`refresh` 附 `cand_disclaimer`："主力动向观察清单，机械归类，非荐股非买卖信号"；`today` 行附 `quality_pct`(quality 共振分位穿透标注,索引空→None,见 cross-list-annotate B3;命中默认参数时另经 tracker 落 `list_track` 头部 top20 追踪,见 sorting-diagnosis)；`channels` 返回 `smart_money.channel_status()` 各通道 ok/err/rows/date，叠加 DB 实况——最新日期有行即 ok=True，防容器重启后内存态 `CHANNEL_STATUS` 全归零「未采集」明明有数据却全显灰）`/api/smart-money/seats`（游资营业部龙虎榜统计，席位级按需 on-demand：上榜次数/买入额/卖出额/净额，period∈近一月/近三月/近六月/近一年，附 `cand_disclaimer`）`/api/smart-money/seats-stocks`（游资追逐个股，席位明细按需 on-demand：对该日龙虎榜个股逐个取买入席位，慢 N股×1调用，仅个股 ETF不上龙虎榜，附 `cand_disclaimer`）→ `/api/smart-money/behavior`（主力行为时间序列：连续净流入/流出天数·累计净额·边际加速·多通道 daily，只读 `smart_money_action` 近 N 日，附 `cand_disclaimer`："主力行为序列机械统计，非买卖信号，盈亏自负"）→ `/api/smart-money/phase?code=`（主力阶段计分判定:吸筹/洗盘/拉升/出货/观望 + confidence + triggers,复用 behavior_series+chip+spot,30s缓存,附 `cand_disclaimer`："主力阶段机械判定(计分制),研究观察非买卖信号,盈亏自负"）`/api/smart-money/seat-winrate?actor=&k=&days=`（席位历史胜率:该 actor 龙虎榜上榜个股前视 k 日收益中位数/胜率,多 k 5/10/20,国家队展开,依赖 `scripts/backfill_lhb_history.py` 回填,附 `cand_disclaimer`："席位历史胜率统计事实,非预测,盈亏自负"）`/api/smart-money/board-link?code=`（板块-个股资金联动:所属板块净流入排名+板块内 net_intensity 位置+5日趋势,附 `cand_disclaimer`："板块-个股资金联动机械统计,非买卖信号,盈亏自负"）`/api/smart-money/radar?days=&market=&limit=`（多通道主力共振雷达:按 code 聚合各通道 daily/连续净流入流出天数/累计净额/边际加速,只读 `smart_money_action` 不触网,股数通道与金额通道量纲分开绝不混比,附 `cand_disclaimer`："多通道主力共振机械雷达,非买卖信号,盈亏自负"）→ `/api/track/fill`（清单历史追踪回填:对 `list_track` 中 quality/nextday 默认参数记录的每日清单幂等回填 T+1 open 买入/T+1+k close 卖出前视收益,仅统计事实不预测）→ `/api/chip`（个股筹码分布：移动成交量加权成本 avg_cost/获利盘·套牢盘比例/chip_concentration/chip_range_90/分箱直方图，依赖 `stock_daily` 历史(需先 `/api/backtest/fetch` 拉该 code)，无历史→`need_history=True`，**不入库不触网**进程内算，附 `cand_disclaimer`："移动成本分布机械统计，非支撑/压力位预测，非买卖信号，盈亏自负"）→ `/api/tdx/quote`（单股实时五档行情：通达信 TCP 7709 直取，盘中实时免 key，`get_quote` 返 price/last_close/open/high/low/vol/amount/bid1-5/ask1-5，附 `cand_disclaimer`："实时行情机械快照，非买卖信号"）`/api/tdx/quote-analysis`（结构化盘口观察报告，支持代码/名称，附 `cand_disclaimer`，不等同主力资金流）`/api/tdx/company-info?code=&category=`（通达信公司信息文本块按需取：16 类[龙虎榜单/主力追踪/股东研究/财务分析/股本结构/研究报告 等]，**第一版返原始文本不解析**，前端 pre 预格式显示；注意通达信"龙虎榜单"类别实含【融资融券/资金流向/异动/大宗交易】非游资席位龙虎榜，附 `cand_disclaimer`）→ `/api/stock-search`（股票代码/名称模糊搜索，查 `stock_spot` code LIKE OR name LIKE，供前端输入框自动补全，机械查询非荐股）→ `/api/st-list`（ST 全名单快照，默认 disclaimer）→ `/api/management`（高管增减持，channel=高管增减持，附 `cand_disclaimer`）→ `/api/share-unlock`（限售解禁按 as_of 月份查，附 `cand_disclaimer`）→ `/api/research`（研报评级，近 N 日，附 `cand_disclaimer`："机构研报评级机械汇总，非荐股非买卖信号"）→ `/api/comments`（千股千评 per-code 按需+缓存 source=comments，附 `cand_disclaimer`）→ `/api/quality`（多口径共振优质筛选，附 `cand_disclaimer`："多口径共振机械排序观察清单，非荐股非买卖信号"；复用 buffett/signals/smart_money/candidates 因子，不新增表；依赖 `*_daily` 历史的口径[风险调整/多信号]未 fetch 则空并降级 `min_dims`；新增 `strict_quality`(默认 true)/`min_confidence`(默认0.50)/`risk_penalty`(默认 true) 可选参数，严格模式过滤低可信度/未过硬门槛标的进 main，低可信度保留 in by_dim，全失败→`selection_mode="degraded"`；item 附 `raw_resonance`/`adjusted_resonance`/`data_confidence`/`confidence_level`/`risk_flags`/`warnings`/`hard_gate_pass`/`sector_heat`/`policy_hit`(行业景气排序上下文,仅标注不改共振)）→ `/api/nextday-strong`（次日强势，2026-09-06 重构版：五步漏斗诊断+五因子连续评分；参数 `min_change_pct/min_turnover/max_price/min_mv/max_mv/max_pe/exclude_st/selection_mode(strict|score)`；排序 score=五因子横截面 rank-pct 加权重归一(mom_5_1 0.30/rel_strength 0.20/sr_10 0.15/close_vol_corr 0.10/liq_turnover 0.15，IC 校准)；strict=五步全通过才进 items 按 score 排序，全失败→`selection_mode="fallback"` 返回接近通过的诊断清单+note；score=纯因子分排序；响应含 `passed_items/all_items/rejected_items`+逐股 `step_status/step_reasons/data_quality/factor_scores/score_coverage/quote_available/data_source`+穿透标注 `sector_heat`/`policy_hit`(行业景气上下文)/`mf_phase`/`mf_confidence`/`streak_inflow`/`cum_net`/`margin_accel`/`north_cum`/`behavior_group`(主力阶段,仅 passed_items,见 cross-list-annotate A+B2)；**触网**：tdx get_quote 盘口增强+`_fill_missing_history` 缺历史补~180日+`_board_members_batch` 板块成分股(tdx `get_block_members` 优先→board_stocks 降级)，30s进程缓存，附 `cand_disclaimer`："次日强势清单——5因子机械排序观察清单，非荐股非买卖信号，盈亏自负"）→ `/api/daily-strong`（**旧 URL 兼容入口**，转调 `screener.daily_strong.daily_strong_rank`，前端已退役；每日强势5步漏斗+板块助攻：step1入选[涨幅>5%/换手>3%/价<50]+step2雷区[市值10-200亿/PE≤150非亏损/非ST]+step3形态[多头排列或放量突破60日线]+step4软打分[量比/涨幅温和]+step5板块助攻[行业热度前5+≥2涨停]；混合硬剔除+软打分，复用 stock_spot/sector_fund_flow/stock_daily，step3 复用 signals._uni_panels 取数本地算MA，无历史该步降级，30s缓存，附 `cand_disclaimer`："每日强势清单——多步机械漏斗+板块助攻排序观察清单，非荐股非买卖信号，盈亏自负"）→ `/api/stock-analysis?code=`（个股深度分析卡：**inline 聚合在 server.py，无独立模块**——聚合 buffett 基本面+评分(0-100 机械：ROE/毛利率/负债/FCF/护城河/估值-红旗)+主力动向(近30日各通道净额/每日 sparkline)+研报评级+千股千评+技术信号(scan_signals)+多因子机械预判 `outlook`：**连续加权版**——`WEIGHTS`(基本面0.30/资金面0.20/技术面0.20/机构评级0.15/估值0.10/内部人0.05)×连续 `strength∈[-1,1]`→`score`∈[-1,1]，`score>0.15`偏多/`<-0.15`偏空/else中性，`confidence`=|score|×100；`reasons` 每条带 `weight`/`strength`，`contribs` 记每因子加权贡献；附 `bt_disclaimer`："基于公开数据的多维机械分析+多因子预判,研究优先级非买卖信号,盈亏自负"）。新增路由时按所属域挂对应 disclaimer。根路由 `/` 返回启动信息 + `/docs`/`/web` 指针 + `disclaimer`；`/web` 静态托管前端（同源，免 CORS）。

> 交互调试：FastAPI 默认 Swagger UI 在 `/docs`（ReDoc 在 `/redoc`），可手工调任意路由、看 schema 与 `disclaimer` 字段。

### 关键设计决策（非显而易见，改前必读）

**AKShare 数据源在当前出口 IP 被东财封**（`RemoteDisconnected`/502）。代码已加备援，改动时勿破坏：

- `data/collector.py` 顶部 `_install_http_patch()` 全局给 eastmoney 域名注入浏览器 UA + Referer，并对 `RemoteDisconnected`/502/503/504 退避重试 4 次。**必须保留**——akshare 的板块接口 `requests.get(url, params=params)` 不带 headers，默认 `python-requests/*` UA 被东财 502。
- 板块采集：东财 `stock_board_industry_name_em` 失败 → `stock_board_industry_summary_ths`（同花顺，字段：板块/涨跌幅/净流入/上涨家数/下跌家数/领涨股/领涨股涨跌幅）。概念：东财失败 → `stock_board_concept_summary_ths`（仅龙头股/成分股数量/驱动事件，**无涨跌幅/资金流**——这是已知限制）。
- 资金流 `ak.stock_sector_fund_flow_rank`：`sector_type` 合法值是 `行业资金流/概念资金流/地域资金流`（**不是** `行业/概念`），`indicator` 只支持 `今日/5日/10日`（**无 20日**）。`collector._SECTOR_TYPE_MAP` 做了简称映射，对外仍接受 `行业/概念`。
- 历史日线：**通达信主源**(`_fetch_hist_tdx` 取 raw → `adjust.qfq` 本地算前复权,标 `source=tdx主源,本地qfq`)，akshare 新浪 qfq(`fund_etf_hist_sina`/`stock_zh_a_daily`)降为备援(东财历史接口全被封,新浪偶可用)；板块走 `stock_board_industry_index_ths`（best-effort，THS symbol 映射可能不全）、基准 `stock_zh_index_daily`。**pytdx `fenhong`/`songzhuangu`/`peigu` 为"每 10 股"单位(非每股)**——`adjust.get_xdxr` 归一化时 ÷10,`qfq` 直接用每股值(勿在 `qfq` 里再 ÷10,否则过度复权导致除权日跳变)。`qfq` 算法:先用 raw 收盘算全部 ratio(避累乘污染前收)→再按除权日降序把 ratio×到对应位置之前的所有价格、volume ÷扩股比。

**NaN→None 序列化**：`screener/engine.py` 两处与 `collector._to_records` 必须用 `df.astype(object).where(pd.notna(df), None)`。**不能**用 `df.where(pd.notna(df), None)`——对 float64 列 None→NaN 又变回 NaN，starlette 的 JSONResponse `allow_nan=False` 会直接 500（`Out of range float values`）。

**日期范围过滤**：`data/history._filter_range` 与 `backtest/eval.load_panel` 用 `pd.to_datetime` 比较，**不能**用字符串比较——数据日期是 `2024-06-01` 带破折号、API 参数是 `20240601` 无破折号，字符串比较会让 2023 全排除/2024 全纳入。

**DB 迁移**：`db.init_db` 先 `executescript(SCHEMA_SQL)`（`CREATE TABLE IF NOT EXISTS` 不会给已存在的表补列），再 `_migrate` 对旧库 `ALTER TABLE ADD COLUMN`（try/except 忽略 duplicate column）。新增列时同时扩 `_BOARD_MIGRATIONS`（或类比的新表迁移）——旧库在持久化卷里，否则字段缺失。

**pandas 3.0 兼容**：`pd.Grouper(freq="M")` 已弃用→用 `ME`/`QE`（`backtest/engine.rebalance_dates` 有 `_map` 归一）；`Series.reindex` 的 fill_value 必须用关键字（`b.reindex(comb, fill_value=0)`，不能位置参数）。

**回测因子可历史重建的只有 OHLCV 派生**（`momentum_n/volatility_n/turnover_n/activity/momentum`）。spot 快照因子（`up_count/down_count/main_net_inflow/leading_stock_change`）是当日盘后聚合、无历史序列，**无法回测**——改 `BACKTEST_FACTORS` 时勿把 spot 因子塞进去。

**signals/portfolio/buffett 的数据依赖链**：
- `signals.scan` 依赖 `*_daily` 历史表（个股 `stock_daily` 等），**必须先 `/api/backtest/fetch` 拉过对应 codes 的历史**，否则空表无信号。RSI/MA 全部在 `signals.py` 本地重算，不入库。
- `buffett.fetch_abstract` **tdx 主源**(`fundamentals.parse_tdx_financial` 解析通达信"财务分析"文本→摘要宽表+三大表)，akshare `stock_financial_abstract` 降备援(仅 tdx 解析失败 parsed=None 时触达，实测 tdx 返 base dict 几乎不触达)。tdx 缺某源(数据缺口)→写空 df 哨兵使 `fundamentals.fetch` 命中返 None 秒回不重 parse。**`analyze_many` 前串行 `prefetch_financial(codes)` 预热填缓存**(单线程避免 8 worker 并发争用 pytdx 单 TCP 连接 Lock 致 parse 0.15s→1-3s 抖动)，并行阶段全缓存命中 0 次 pytdx。`_spot(code)` 用 `where code=?` 走主键索引查单行(旧无 where 全表扫 5200 行×8 worker 并发致 GIL/内存硬 stall)。容器实测 80 只 shortlist：cold 40s/16→23.5s/72，warm 40s/0→9.5s/72；/api/quality 冷 67s(<75s 不再 AbortError) 暖 0.655s。缓存键坑：`fundamentals._cache_get/_cache_set` 必须 `_strip_prefix`（与 `_fetch_net` 对称），否则 fetch_abstract 预填的带前缀 code 与 fetch 查询键不匹配。`_AK_OK=False` 时 akshare 备援块跳过（tdx 仍主源可用）。`/api/buffett/top` 先从候选池 shortlist（依赖 `/api/refresh` 的 spot 快照排除 ST/涨停/停牌）取标的，再逐个拉财报评分。
- `portfolio` 是独立的 `portfolio` 表（`id/code/name/buy_date/buy_price/shares/note/ts`），与行情表解耦；浮盈靠 `stock_spot` 最新价现算，无 spot 时浮盈字段为空。
- **`smart_money_action.date` 列混有未来日期**（限售解禁通道把未来解禁日 `as_of` 也写进 `date`）。`screener/smart_money.today_list` 省略 date 时取"最新日期"**必须封顶 today**（`where="date <= ?"` today），否则 `order_by date DESC` 取到未来解禁日（如 2026-08-31），`date BETWEEN start AND 未来` 把今日真实资金流/龙虎榜全过滤掉，today 视图只剩未来解禁行——即"深查主力没效果"的根因。改 `today_list` 默认窗口逻辑时勿去掉这层封顶。

## 改动检查清单

> **设计文档**：`docs/superpowers/specs/` 当前存 27 份设计 spec，`docs/superpowers/plans/` 当前存 30 份实施 plan；按要改的领域先读对应文档，拿到”为什么这么设计”的上下文比读代码回溯快。`backtest-realism`、`chip-distribution` 目前仅有 spec；其余文档以目录实际文件为准。

- 新增 ETF/QDII 长短清单(`screener/etf_screen.py`) → **`etf_spot` 表无 nav/fund_scale 列**，净值/规模/费率/估值必须经 `_fetch_qdii_premium`/`_fetch_quality_meta`/`_fetch_index_valuation` 抽象按需取（`fund_etf_spot_em` 主源,失败/无 IOPV 诚实 None 不硬造），绝不直接读不存在的表列；item 统一带 `name`（本地占位==code 时经 `_display_name` 回退全市场快照真实名,无源诚实保留占位前端显"待刷新"）；请求含 universe/mode/codes/limit/days 全部入 30s `_CACHE` 缓存键；QDII 高溢价 long 降权标 risk_flag、short 顶部——措辞"长/短清单机械排序观察清单非荐股非买卖信号",挂 `cand_disclaimer`。同步 `tests/test_etf_screen.py`(数据源全 mock 不触网)。
- 新增 SQLite 列/表 → 同步 `models.SCHEMA_SQL` + `TABLE_FIELDS` + `db._BOARD_MIGRATIONS`（若旧表补列）。
- 新增采集源 → 复用 `(df, ok, err)` 返回约定 + `_to_records`（NaN→None）+ 异常不抛崩；eastmoney 域名自动受 HTTP patch 保护。
- 新增 AKShare 采集源 → 列表型(stock_zh_a_st_em/stock_hold_management_detail_em/stock_restricted_release_detail_em/stock_research_report_em)进 refresh_all/refresh_today;per-code(stock_*_sheet_by_report_em/stock_comment_detail)走 fundamentals.fetch + fundamentals_cache 缓存(7天 TTL,超时降级 stale);复用 `_first_col` 候选名容错,单测 mock 验证字段映射。高管增持仓/限售解禁并入 smart_money_action 复用 channel 零加列;限售解禁 as_of=真实解禁日期(可能未来),查询按 as_of 月份(unlock_by_month)。
- 新增 finshare 备援源 → `data/smart_money.py` 的 `collect_dragon_tiger` 东财主源(`stock_lhb_detail_em`)空或异常(被封)→ finshare `get_lhb()` 备援(不走东财 push2 被封端点,实测可用;净买/买/卖额拆分比东财主榜单更全)。finshare 函数内 `import`(独立于 akshare,`_AK_OK=False` 也走备援);多日榜单按 `trade_date`(无破折号 YYYYMMDD)经 `_norm_date` 过滤当日;`fs_code`('000004.SZ'/'SH600519'/'sz000004')经 `_fs_code_norm` 归一 6 位纯代码;finshare lhb **无 name 列**→记录 name 留空,下游 `stock_spot` 按 code 补;两边皆无当日上榜→成功空(不算错),两边败→False 诚实标 `source=finshare`。**finshare 资金流接口(`get_money_flow`/industry/concept)同源东财 push2 被封→不可作资金流备援**(THS 直取仍是唯一可靠全市场源);finshare 2.1.0 已移除北向函数→北向需另选源(a-stock-data 官方交易所备胎)。但 `get_money_flow_stock(code)` 个股历史主力净额序列**不走东财被封端点(实测可用)**→`screener/smart_money.behavior_series` 资金流通道表无记录时按需补(标 `source=finshare`,截近 days 日,仅个股分析卡/behavior 按需调,不进 refresh 全量避免 5200 次调用)。`requirements.txt` 加 `finshare>=2.0.0`。
- 新增 pytdx 直连源(通达信行情服务器,免 key TCP 7709,不走 akshare) → `data/pytdx_client.py` 薄客户端:服务器池轮询+单例复用(`_get_api` 带 `threading.Lock` + 心跳重连,FastAPI 多线程安全)+`_market`(5/6/9→沪=1,0/1/3→深=0[1 开头=深市 ETF],4/8→北交=2 未实测失败降级)+`_project_symbol`(复用 history._sina_symbol 规则,保证落 stock_daily.symbol 列一致)。`get_quote(codes)` 批量五档实时(≤80/批分页,NaN→None 复用 `_nan`);`get_daily_bars(code,count)` 日 K(**不复权**,`_BATCH=800` 偏移分页累加,列映射 vol→volume/datetime→date 截 YYYY-MM-DD,drop_duplicates+sort);`get_company_info(code,category)` 公司信息文本块(16 类,**第一版不解析返原文本**,通达信"龙虎榜单"类别实含【融资融券/资金流向/异动/大宗交易】**非游资席位龙虎榜**,后者走 finshare/`/api/smart-money/seats`);`get_block_members(category)` TDX 板块成分股文件(industry/concept/all,nextday `_board_members_batch` step5 用,TDX 行业文件常缺部分板块→缺失者逐个降级 `board_stocks.fetch_constituents`)。**不进 refresh_all**(TCP 按需同 history)。**history.fetch_stock_hist/fetch_etf_hist 以 tdx 为主源**:`_fetch_hist_tdx` 取 raw → `adjust.qfq` 本地算前复权(标 `source=tdx主源,本地qfq`),akshare 新浪 qfq 降为备援(tdx 空/失败/连不上时);`get_daily_bars` 旁加 `get_xdxr` 包装取除权除息因子。**pytdx xdxr `fenhong`/`songzhuangu`/`peigu` 为"每 10 股"单位**,`adjust.get_xdxr` ÷10 归一化为每股,`qfq` 直接用每股值(勿再 ÷10,否则过度复权、除权日出现 4.x→6.x 的跳变)。复权失败(无 xdxr/异常)退化为不复权,诚实标注。tdx 单例连接受网络抖动影响,长 count(多批分页)偶发连不上→上层自动降级 akshare 备援;portfolio.list_positions spot 缺价→pytdx 兜底 latest_price(单测需 mock `get_quote` 避免真连网);stock_analysis inline 加 `latest_price`/`quote` 字段。路由 `/api/tdx/quote`(单股实时五档)+`/api/tdx/company-info?code=&category=`(按需文本),挂 `cand_disclaimer`:"通达信行情/公司信息机械汇总,非买卖信号,盈亏自负"。`requirements.txt` 加 `pytdx>=1.70`。
- 板块成分股(`data/board_stocks.py`) → **按需实时拉取不入库**(区别于 refresh_all 列表型源)；`fetch_constituents(board,category)` **优先东财直取**(`stock_board_industry_cons_em`/`stock_board_concept_cons_em`，`symbol=板名` 直接取无需 name→code 映射，板名来自东财 `stock_board_industry_name_em` 时同源最准)，东财被封/akshare 未装/返回空→降级 THS 静态表(`_fetch_map` 建 name→code 映射 + `_fuzzy_match` 包含/被包含取最长命中，兜底近似板名)；两源都失败→抛 ValueError 由路由 catch 降级(返回龙头股无成分股，诚实标 `cons_error`)；每行带 `source`(em|ths)，路由 `source` 字段从首行派生不再硬编码。东财路径调 `collector._install_http_patch()` 注入 UA/Referer+重试(否则默认 UA 被 502)；翻页至空或行数<20 判末页；read_html 把代码列读成 int 丢前导零(002966→2966)→**必须 `zfill(6)` 补回**(两源同)；`_prefix` 加交易所前缀(6→sh/0/30→sz/920/43/83/87/8→bj)；NaN→None(复用 `_f`/`_s`，防 `allow_nan=False` 500)。**`pd.read_html` 必须显式 `flavor='lxml'`**——pandas 3.0 默认 flavor 走 bs4→html5lib，html5lib 未装即 `ImportError`(板块成分股报错降级、`data/smart_money.py` THS 资金流 `_rows` 静默返回 `[]` 资金流通道空)；`lxml` 已 pin `requirements.txt`。同坑见 `smart_money.py` 的 `_rows`(两处都要 flavor='lxml')。

  > **AKShare 版本适配（2026-07-23 修正，akshare 1.18.64）**：旧函数大面积改名，按旧名调用直接 `AttributeError` 导致 5 通道零写入（DB 只剩资金流）。映射：龙虎榜 `stock_lhb_detail_em(start_date,end_date)` 日期须**无破折号 YYYYMMDD**（带破折号内部 NoneType 崩）；席位明细 `stock_lhb_stock_detail_em(symbol,date,flag)` 现需 date+flag、逐股两次请求太慢，**改用主榜单"龙虎榜净买额"出个股级记录**。十大股东 `stock_gdfx_free_top_10`→`stock_gdfx_free_top_10_em(symbol=sh/sz+code, date=报告期YYYYMMDD)`（`_prefix_code`/`_latest_report_period`）。高管 `stock_hold_management_em`→`stock_hold_management_detail_em()`（无参、17 万行全历史、拉取约 4-5 分钟，`_filter_recent` 按"日期"列取近 7 日，记录 date 取行内变动日期）。限售 `stock_share_change_em(symbol=月)` 已下线→`stock_restricted_release_detail_em(start_date,end_date)` 按日期范围拉个股清单（actor 取"限售股类型"，无"解禁股东"列）。北向两东财端点均 `NoneType` 崩（反爬），无备援，保持优雅失败灰显。`_clean` 已支持 date/Timestamp→字符串（raw 列含上榜日/解禁时间等 date 对象，否则 JSON 序列化 500）。
  >
  > **资金流通道 THS 直取（2026-07-23）**：原口径复用 `stock_spot.main_net_inflow`，但该字段来自东财个股资金流（`stock_individual_fund_flow_rank` 出口 IP 被封 RemoteDisconnected），新浪 `stock_zh_a_spot` 又无该列 → 资金流通道长期停摆。`collect_fund_flow` 改走**同花顺直取**：`_fetch_ths_individual_fund_flow` 绕过 akshare `stock_fund_flow_individual` 的列名 bug（akshare 硬编 10 列、THS 即时表实际列数已变致 `ValueError: Length mismatch`），直接取 `data.10jqka.com.cn/funds/ggzjl/...` ajax 分页，`hexin-v` token 复用 akshare 内部 `_get_file_content_ths('ths.js')`+`py_mini_racer`（模块级缓存 TTL10min,失效重算;分页 `ThreadPool` 8 并发,~70s→5-8s），`read_html` 按表头取 `股票代码`/`股票简称`/`净额(元)`，`_parse_cn_amount` 解析"822.74万"/"1.63亿"/负数。THS 失败再回落 spot.main_net_inflow（东财残量，通常空）。THS 不封东财 IP，是资金流通道唯一可靠源（~5200 行/当日，拉取约 1 分钟）。
- 新增 API 路由 → 用 `_wrap()`，回测/候选池类附对应 disclaimer。
- 新增/修改清单追踪(`backtest/tracker.py`) → 记录入口须用 `is_default_params(module,params)` 判默认参数才落库 `list_track`，避免任意参数污染统计；收益回填幂等（重跑不覆盖非空收益字段，`filled_ts` 标记）；依赖 `stock_daily` 历史需先 `/api/backtest/fetch`。改默认参数时同步 `DEFAULT_PARAMS` 与 `tests/test_tracker*.py`。主力雷达(`screener/smart_money.radar` + `/api/smart-money/radar`)只读 `smart_money_action` 不触网，注意**股数通道与金额通道量纲分开**（十大股东/高管/限售=股，资金流/龙虎榜/北向=元），绝不混入同一合计。新增/修改 quality 编排层 → **不新增表**（复用 stock_spot/etf_spot/*_daily/smart_money_action），因子源文件 buffett/signals/smart_money/candidates **只读结果不改**；口径分位缺失为 None（`hits` 不计），任一因子源失败标 `dim_status=err` 不崩；`*_daily` 未 fetch 时口径1/4 空并 `min_dims` 自动 clamp。盘口精排阶段(pytdx get_quote)守同约束:仅 universe=stock+refine+小名单(top refine_pool=50)触网,非全市场;A 流动性深度进综合分(0.6共振+0.4流动性),B/C 方向不进排序仅 raw(合规);盘后 in_session=False 自动降级 A/B=None 仅 C;_is_in_session 注入 now 可测;改权重/阈值同步 tests/test_quality_refine.py(17 测试)。**行业景气加成层**(`screener/sector_heat.py` + quality 精排 `_apply_sector_heat` + smart_money `_sector_ctx` 标注):`board_heat` 保持纯函数(0.6*资金+0.4*上涨宽度 rank-pct),**不改共振签名**(_DEFAULT_DIM_WEIGHTS/min_dims/hits/缓存键零波及);个股归板块**必须**用 `nextday._board_members_batch` 反查(stock_spot 无 board 列),且只传"被评分板块"(sector_fund_flow行业今日∩industry_board 名)有界单次调用,勿全板块扫;policy 命中用静态 `POLICY_THEMES`;精排 `_final` 盘中=0.6*res_pct+0.2*quote.liquidity_pct+0.2*heat_pct+policy(liq 用纯流动性分位,勿用 `_refine_score` 当 liq 代理——它含 0.6res 会重复计入),盘后=0.8*res_pct+0.2*heat_pct+policy;同步 tests/test_sector_heat.py(7 测试)。**网络尾段硬墙钟(2026-09-19)**：quality_rank 的盘口精排+板块反查尾段(原 inline 块,现 `_refine_tail_body`/`_run_refine_tail`)只有 try/except 捕不住"慢而不报错"的死网络挂起——pytdx 死池逐个走 5 台服务器(_TIMEOUT=8×5≈40s)、板块降级再逐板块 THS(每页 timeout=15)，冷缓存首请求总耗时能 >客户端 120s/150s 双 AbortError。修复：尾段套 daemon 线程 + `queue.get(timeout=_REFINE_TAIL_DEADLINE=25)` 硬墙钟，超时返 None → 保留未精排主清单 + `refine_status="skip(网络尾段超时,保留未精排清单)"` 诚实降级，后端必在 buffett(≤40)+尾段(≤25)+其他(≤20)≈85s 内返回。与 board_stocks._fetch_constituents_em_timed 同范式(daemon 线程 + queue 硬超时)；daemon 线程超时后继续后台自担(pytdx _lock 可能被僵尸占着,后续请求 acquire timeout=8 即降级)。改 deadline 阈值同步 `tests/test_quality_refine.py` 新增 `test_quality_rank_refine_tail_timeout_degrades`(patch 0.3s deadline 测超时降级快速路径)。**主清单新政接入(2026-09-21)**：精排 `_apply_sector_heat` 只在 refine 成功时跑，refine=False/ETF/超预算/尾段超时降级分支无政策排序——`quality_rank` 在 `_apply_combo` 前对"未精排"主清单(`refine_status` 不以 ok 开头)补跑 `_apply_policy_bonus`(套 `_POLICY_DEADLINE` 硬墙钟 + 独立在途守卫，复用 `nextday._board_members_batch` 有界反查)，附 `policy_hit/style_hit/policy_event` 并按其重排(不含流动性)，失败/超时保留原排序诚实省略。**三清单穿透标注**(cross-list-annotate,2026-09-18)：quality 精排给 main 附 `mf_phase`/`mf_confidence`/`streak_inflow`/`cum_net` 等(`_enrich_main_behavior`),并产 `_RES_PCT_INDEX`(code→共振分位,lazy import 避循环)供 smart_money 清单读;nextday passed_items 附 `sector_heat`/`policy_hit`(A)+`mf_phase`/`streak_inflow`(B2)。约束:穿透字段只取对端轻量/可批量/有缓存信号,绝不重跑对端全量(buffett/nextday),None 诚实缺失**不进排序**,命名统一用 `mf_phase`(非 `main_phase`),lazy import 避 quality↔smart_money↔nextday 循环依赖;smart_money 端接 `_attach_quality_pct` 于 top_by_amount/today_list。
- 新增 smart_money 追踪/诊断(sorting-diagnosis,2026-09-19) → `backtest/tracker.py` `DEFAULT_PARAMS` 加 `smart_money`,`record_list` 白名单与 `_meta_payload` 对齐;server `/api/smart-money/today` 命中默认参数时仅头部 top20 落 `list_track`(mode=today,score=净额,meta=quality_pct),**不改变响应、零排序副作用**。诊断走 `scripts/diagnose_ranking.py`(只读 `list_track` 前视收益,头部 vs 整体/五因子分档/穿透分层/quality 通过率结构),**不触网不改排序**,仅在样本积累后人工重跑。改默认参数同步 `tests/test_tracker*.py`/`test_diagnose_ranking.py`。
- 新增/修改 nextday 次日强势编排层 → `screener/nextday.py` `nextday_strong_rank(universe,codes,limit,days,min_change_pct,min_turnover,max_price,min_mv,max_mv,max_pe,exclude_st,selection_mode)`。**范式(2026-09-06 重构,改前必读)**：旧 daily_strong 五步漏斗(step1 强势门槛[涨幅/换手/价]+step2 风险硬剔除[市值/PE/ST]+step3 MA 形态+step4 量价软分+step5 板块助攻)已**并入本模块**,五步仅作**诊断解释+strict 选择**(`step1_pass`..`step5_pass`/`step_status`/`step_reasons`/`failed_steps`,由 `_explain_step_status` 生成),不再主导排序;排序主键=**五因子连续评分**:mom_5_1 0.30/rel_strength 0.20/sr_10 0.15/close_vol_corr 0.10/liq_turnover 0.15(IC 校准 2026-09-06,692只/1619交易日:仅 mom_5_1 有 1 日强正 IC +0.023 胜率54%→提 0.30;close_vol_corr 双窗口负 IC→压 0.10;sr_10 弱正→降 0.15;rel_strength/liq_turnover 无法用 OHLCV 回测维持;见 `_FACTOR_WEIGHTS` 注释)。每因子先 `_rank_pct` 横截面转 0-1 分位(并列取平均秩,**缺失保持 None 不伪造零分**),再 `_weighted_score` 按可用因子重归一(denom,权重和不必为 1)→score∈[0,1] 降序。`selection_mode`:strict(默认,五步全通过才进 items 按 score 排序,全失败→`"fallback"` 返回接近通过的诊断清单+note)/score(纯因子分排序不看五步)。**触网(区别于旧"不触网"约束)**:①tdx `get_quote` 盘口增强粗筛名单(`_tdx_rich_enrich`/`_tdx_vol_ratio`,失败诚实缺失 `quote_available=False`/`data_source` 标注);②`_fill_missing_history` 对缺 `stock_daily` 历史的候选按需拉 ~180 自然日 tdx 日K(8 并发 fetch+**串行 upsert** 避 SQLite 写锁,覆盖 MA60/20日量均/量比);③`_board_members_batch` step5 板块成分股:tdx `get_block_members("industry")` 优先(板名精确/包含匹配),缺失板块**逐个**降级 `board_stocks.fetch_constituents`(勿用 any() 短路整体降级——TDX 板块文件常缺)。**不再依赖 smart_money_action/chip_distribution/main_force_phase**(旧资金连续/主力阶段/筹码收集因子已移除)。粗筛 top 200(change_pct 降序,codes 限定时不粗筛)+30s进程缓存(键含全部参数含 selection_mode)+NaN→None 守卫(`_nan`)。改因子/权重/步骤同步 `tests/test_nextday.py`(50 测试)。措辞个人自用放松"次日强势清单",挂 cand_disclaimer。
- 新增 daily-strong 每日强势编排层 → `screener/daily_strong.py` `daily_strong_rank(universe,codes,limit,days,...)` 5步混合编排（**2026-09-06 起五步漏斗已复制并入 `nextday.py` 作诊断层，本模块仅供 `/api/daily-strong` 旧 URL 兼容转调，前端 dsLoad/dsRender 已退役；改五步语义时两处需同步或明确只改 nextday**）(step1入选/step2雷区/step3 MA形态/step4软打分/step5板块助攻,**硬剔除+软打分**,与 nextday 打分式范式不同)→排序键=硬通过数×10+软分降序;**不触网不新增表**复用 stock_spot/sector_fund_flow/stock_daily;step3 复用 `signals._uni_panels` 取 close 面板(仅取数不复用触发语义)+本地算5/10/20/60MA判多头排列/放量突破/空头/粘合;step5 复用 `board_money_link` code→board 反查套路(内联,不调 board_money_link 避免多算 net_intensity);粗筛 top K(≤200)+30s缓存+codes限定;无历史/无board降级不崩;NaN→None守卫(`_nan`)。依赖 stock_daily(step3,需 /api/backtest/fetch)+sector_fund_flow(需 /api/refresh)。同步 tests/test_daily_strong.py。措辞"每日强势/板块助攻",挂 cand_disclaimer。
- 改 `/api/stock-analysis` 个股分析卡 → **逻辑 inline 在 `server.py`，无独立模块**（区别于 buffett/signals/quality 等领域模块）；聚合顺序：buffett.analyze→评分(0-100 机械)→`db.query_rows("smart_money_action")` 近30日→research 研报+千股千评→`scan_signals`→多因子预判 `outlook`。改预判因子权重/阈值直接改 server.py 内 `stock_analysis()` 的 `WEIGHTS` 与 `_push` 闭包；每段 try/except 独立兜底，单源失败不拖垮整卡。措辞保持"多维机械分析/研究优先级非买卖信号"，挂 `bt_disclaimer`。
  > **outlook 多因子预判连续化（2026-08-13）**：`_push(factor,d,detail,strength=None)` 用连续 `strength∈[-1,1]` 替原 ±1/0 三态（`None` 时按 dir 退化为 ±1/0，触发型因子兼容）；`contrib=weight×strength`，`score=Σcontrib∈[-1,1]`，`label` 阈值 ±0.15 不变，`confidence=min(|score|,1)×100`。六因子 `WEIGHTS`={基本面评分0.30/资金面0.20/技术面0.20/机构评级0.15/估值0.10/内部人0.05}，**合计=1.0**（基本面从 0.35 挪 0.05 给内部人，权重为经验先验未经回归校准）。各因子 strength 来源：基本面 `(card.score-50)/50`（score 由 step1 从 ratios 机械算，非 ba.score）；估值 `tanh(margin_of_safety×3)`（MoS 来自 buffett，无 MoS 则回退 valuation_tag 三态）；资金面调 `sm_query.behavior_series(code,30)` 用 `streak_inflow`/`streak_outflow`（`streak≥5` 才触发，强度 `tanh(streak/8)`，**替原 raw 净额合计**——单日大额不再误判偏多）；技术面/机构评级命中数 `min(count/3,1)`；**内部人因子**查 `smart_money_action where channel='高管增减持' AND code=? AND date>=近30日` 净额方向（注意 channel 值在 `params` 里不在 `where` 串里，mock 时按 params 判别）。改 outlook 因子/权重时同步 `tests/test_outlook_continuous.py`（8 测试 TestClient+全 mock，`_RATIOS_HIGH/LOW` 控制 card.score）。
- 新增筹码分布/主力行为序列 → `screener/smart_money.py` 的 `chip_distribution`/`behavior_series` **不新增表、不触网**：chip 复用 `backtest.signals._uni_panels` 取 `stock_daily` close+amount 进程内算移动成交量加权成本(收盘价代理成交价)，依赖 `*_daily` 历史(需先 `/api/backtest/fetch`)，无历史→`need_history=True`；spot 取值优先级 param>`stock_spot.latest_price`>daily 末值；NaN→None 复用 `_nan`/`_weighted_percentile`/`_histogram`(防 `allow_nan=False` 500)；`behavior_series` 只读 `smart_money_action` 按 channel(资金流/北向/龙虎榜) 聚合到 code-date 级算连续性/边际(`_streak` 算连续末尾正负天数)。措辞"机械统计/非支撑压力位预测/非买卖信号"，挂 `cand_disclaimer`。
- 自选股(watchlist) → 镜像 portfolio：`data/watchlist.py` 独立 INSERT(不走 `upsert_rows`/`TABLE_FIELDS`)；同 code 重覆加 → UPDATE 不重复插入；`list_items` 按 `id DESC`（不按 `added_ts`，避免同秒并列乱序）；表只加 `CREATE TABLE IF NOT EXISTS watchlist` 进 `SCHEMA_SQL`，**无需进 `_BOARD_MIGRATIONS`**（新表 IF NOT EXISTS 启动即建，迁移只给旧表补列）。前端"＋自选"按钮统一调 `wlQuickAdd(code)`（信号扫描行带 `event.stopPropagation()` 防触发行跳 analysis）。
- 新增前端字段/运算符 → 下拉已从 `/api/fields` 动态渲染，通常无需改 JS；但 `LABEL` 字典要补中文标签。
- 新增 signals/buffett 信号规则 → signals 规则在 `signals.py` 内本地重算（不入库），buffett 评分维度改 `buffett.py` 的 v2 逻辑；两者措辞保持"机械评分/研究优先级/非买卖信号"，按域挂 `cand_disclaimer`/`bt_disclaimer`。signals 已支持 `min_hits`/`stop_loss`/`fee_bps` 研究参数（共振过滤、前视止损、双边费率），改 `_uni_panels` 时须保持默认 `with_ohlc=False` 返回 2-tuple（旧测试 mock 兼容），`with_ohlc=True` 返 4-tuple；前端"信号回测研究台"卡多 k 并行调 `/api/signals/backtest` 渲染 signal×k 矩阵。
- 改 buffett 价值评估口径（**零新数据源**，全用 `fundamentals.fetch(code, balance/cashflow/profit)` 已能取到的完整三大表 + 摘要序列）→ `buffett.analyze` 现含：**所有者收益**（巴菲特 1986：`ni+折旧摊销-资本开支-营运资本增加`，`ratios.owner_earnings`/`owner_earnings_to_ni`，`<0.5` 标红旗"赚非现金"；科目缺失降级现有 FCF 代理，`fcf_source` 标注不变）；**杜邦分解**（`net_margin×asset_turn×equity_mult≈报告ROE`，偏差>3pt 标 `dupont_note`，`equity_mult>3` 标"ROE靠杠杆"）；**ROIC**（`NOPAT=ni+利息×(1-tax_rate)`，`invested_capital=总资产-无息流动负债`简化口径，`>15`高资本回报）；**增长持久性**（`_cagr` 算 rev/eps/bps/ni 多年 CAGR，n≥3 才算；`_consistent_years` 数连续 ROE>15 年数；**`_cagr`/`_pick_row_fields`/`_pick_col_sum` 假设输入序列降序(新→旧)**，mock 必须降序喂）；**轻量 DCF（Gordon-on-Book 剩余收益）**：`g=观测BPS CAGR`（截 `[0,roe_avg/100]`，零新数据源），`r=_COST_OF_EQUITY=0.09`，`justified_pb=(sustainable_roe-g)/(r-g)`，`IV=justified_pb×bps_latest`，`MoS=(IV-price)/IV`；**`g≥r` 模型失效**→`justified_pb=None`+`dcf_note`标失效（两分支都设 `dcf_assumptions` 透明披露假设）；`valuation_tag` 整合 MoS（`>0.3`便宜/`[0,0.3]`合理/`<0`贵），与横截相对分位并存；`rank_top` 预筛加 `owner_earnings_to_ni<0.3` 排除 + `priority` 同档按 MoS 降序 + `_build_reasons` 加 IV/MoS/ROIC 叙事。措辞"安全边际/研究优先级/机械剩余收益估值"，**不用"低估买入/卖出"**，挂 `bt_disclaimer`："基于公开财务数据的机械剩余收益估值，假设 ROE/折现率/增长率，非真实内在价值，研究优先级非买卖信号，盈亏自负"。`_pick_row_fields`/`_pick_col_sum` 判年报须 `.replace("-","").endswith("1231")`（报告期带破折号"2024-12-31"，裸 `endswith("1231")` 尾 4 字符是"2-31"漏判）。改这些口径时同步 `tests/test_buffett_value.py`（15 测试 mock 三大表+摘要，bps 序列必须降序）。
- 单测放 `tests/`，合成数据 mock `db.query_rows`，不依赖网络。`tests/fixtures/` 存真实测试夹具（如 `tdx_financial_600519.json`）。测试文件名≈被测模块（`test_quality_refine.py`→`quality._refine_by_quote`、`test_outlook_continuous.py`→`server.stock_analysis` 的 outlook 预判、`test_tdx_financial.py`→`fundamentals.parse_tdx_financial`、`test_nextday.py`→`screener.nextday`、`test_daily_strong.py`→`screener.daily_strong`），按名定位即可。
- `data/cache/` 是 `smart_money` 高管增持仓 pickle 缓存目录（7 日 TTL），优先 `SCREENER_CACHE_DIR` 环境变量/卷 `/app/var`，勿与代码一起提交。
- 勿提交噪声文件：`*.stackdump`（bash 崩溃转储）、`*.db`/`*.log`/`__pycache__/`、`q.json`/`q20.json`（本地调试导出）。其中 `*.stackdump` 与 `*.db`/`*.db-journal` 已在 `.gitignore`；仓库里曾误提交一个 `bash.exe.stackdump`，已删除。
