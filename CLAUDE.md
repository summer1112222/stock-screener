# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **README.md 已更新**（含路由速查/架构/合规/数据源限制，面向用户）。**开发仍以本文件的"路由速查"和"架构"为准**——CLAUDE.md 含非显而易见的设计决策与改动检查清单，README 面向使用者。

## 合规硬约束（最高优先级）

本项目是**数据筛选/回测研究工具，非投资咨询**。任何改动都必须守住：

- **不荐股、不输出实时买卖点、不承诺收益（含"月 10-20%"这类表述）、不自动下单**。无证券投资咨询资质。
- 所有数据响应经 `api/server.py` 的 `_wrap()` 附 `disclaimer` + `update_time`；回测响应附 `bt_disclaimer`；候选池附 `cand_disclaimer`。
- "候选池"是按因子**机械排序的观察清单**，不是"推荐买入"。措辞必须用"筛选/排序/观察清单"，不得用"推荐/买入/卖出"。
- `config.yaml` 注释、每个模块 docstring 都重申此边界——改它们时保持一致。

## 常用命令

```bash
# 本地运行(宿主需装 pandas；akshare 仅采集层需要)
uvicorn api.server:app --reload --port 8000

# Docker（推荐；Dockerfile 配了清华 PyPI 镜像，否则国内 pip 下载仅 17kB/s）
#   部署优先用 deploy.sh(跑测试→重建→健康检查→模块自检,抓镜像漂移)而非手动 docker cp:
#   bash deploy.sh            # 测试+重建+启动+健康检查
#   bash deploy.sh --skip-test # 跳过测试(快)
docker compose up --build -d            # 重建并后台启动(deploy.sh 内部即此+校验)
docker compose down                     # 停止(保留数据卷)
docker compose down -v                  # 连同 SQLite 卷一起清

# 单测（宿主跑；tests/ 被 .dockerignore 排除，镜像里没有）
# 环境：Python 3.12.10 + pandas 3.0.3 + requests 2.34.2（见 requirements.txt）
# 无 conftest.py / pytest.ini / pyproject.toml——必须在仓库根目录跑，子目录里跑找不到 data 包
python -m pip install -q pytest         # 宿主若无 pytest
python -m pytest tests/ -q
python -m pytest tests/test_engine.py::test_topn_asc -q   # 单个测试

# 触发数据采集（GET 与 POST 均可，浏览器地址栏可直接访问）
curl -X POST http://localhost:8000/api/refresh

# 前端：http://localhost:8000/web/index.html  （FastAPI 同源托管 /web + /api）
```

## 架构

四层 + 两个领域模块：

- `data/` 采集与存储。`models.py`（规范字段集、AKShare 别名映射、SQLite schema）、`db.py`（连接/建表/迁移/upsert/query/meta）、`collector.py`（spot 快照采集：板块/ETF）、`history.py`（历史日线采集）、`portfolio.py`（本地持仓表：记录买入、按 spot 最新价算浮盈）、`watchlist.py`（自选股观察清单：未买入跟踪，按 spot 现价显示，同 code 加重覆更新不重复；表 `watchlist(id,code,name,note,added_ts)` 镜像 portfolio，用独立 INSERT 不走 `upsert_rows`/`TABLE_FIELDS`）、`fundamentals.py`（完整三大财报按需+缓存，7天TTL，超时降级 stale）、`research.py`（研报评级+千股千评）、`smart_money.py`（主力动向采集：龙虎榜席位/十大流通股东/陆股通/个股资金流/高管增持仓/限售解禁 6 通道，每通道 `(records,ok,err)`，东财优先+备援(龙虎榜走 finshare `get_lhb`,不走东财 push2 被封端点)+失败标不可用不崩；`refresh_today` 编排(6 通道 `ThreadPool` 并行采集+串行 `upsert` 避 SQLite 并发写锁;THS 资金流 token 模块级缓存 TTL10min+分页 8 并发 ~70s→5-8s;高管 17 万行 7 日 pickle 缓存 `_cache_dir` 优先 env/卷`/app/var`,命中 4-5min→0)+ `CHANNEL_STATUS`/`NATIONAL_TEAM`）、`market.py`（市场温度采集：涨跌家数比纯 DB+legu 估值+两融余额+估值，4 路降级，入 `market_daily` 表 upsert；`latest()`/`trend(days)`）、`board_stocks.py`（板块成分股采集：东财 `stock_board_industry_cons_em` 出口 IP 被 RemoteDisconnected 封死无备援 → 改走同花顺 thshy/gn 详情页静态表（每页 20 可翻页，read_html 解析，代码列 read_html 丢前导零需 `zfill(6)` 补回），按需实时拉取不入库，7 天 TTL 内存缓存 name→code 映射；`_prefix` 给 6 位代码加 sh/sz/bj 交易所前缀）、`pytdx_client.py`（通达信行情直连薄客户端：免 key TCP 7709，服务器池单例复用+心跳重连+`threading.Lock`；`get_quote` 批量五档实时/`get_daily_bars` 日K(raw 不复权,800 偏移分页)/`get_xdxr` 除权除息因子(供本地算 qfq)/`get_company_info` 公司信息文本(16 类不解析)；**历史日 K 主源**(raw+本地 qfq,见 `adjust.py`)；portfolio spot 缺价→tdx 兜底、stock_analysis 加 latest_price）、`adjust.py`（前复权本地计算：`get_xdxr(code)` 取 pytdx 除权除息记录并归一化为每股(fenhong/songzhuangu/peigu 均 ÷10)+`qfq(raw_df,xdxr_df)` 按**除权日降序累乘 ratio** 算前复权(volume 反向 ÷扩股比)；供 history tdx 主源用,使通达信成历史日 K 自洽主源不再依赖新浪/东财 qfq 接口）。
- `screener/` 实时筛选。`conditions.py`（因子目录 `BOARD_FIELDS_CAT/ETF_FIELDS_CAT`、运算符 `OPS`）、`engine.py`（快照过滤/排序/资金流合并、派生因子 `DERIVED_FIELDS`）、`smart_money.py`（主力动向查询/聚合层，只读 `db.query_rows`：`today_list`(limit 默认 1000 截 top N,0=无限制)/`by_actor`(actor LIKE 下推 SQL,含"国家队"保留词 OR 多名展开)/`top_by_amount`(附 `_attach_intensity` net_intensity=主力净额/当日成交额 强度归一化,操盘手跨市值可比维度,只读 `stock_spot`);另含 `chip_distribution`(P1 筹码分布，移动成交量加权成本，**不入库不触网**进程内按需算，复用 `backtest.signals._uni_panels` 取 `stock_daily` close+amount)/`behavior_series`(P2 主力行为序列，读 `smart_money_action` 按 channel 聚合算连续净流入/流出天数·累计净额·边际加速·daily 序列;资金流通道表无记录时按需 `finshare.get_money_flow_stock` 补历史主力净额序列(标 source=finshare,不走东财被封端点),**补到的序列落库缓存**(upsert `smart_money_action`,下次同 code 直接 DB 读避免重拉,属按需单股写非 refresh 全量,与"不进 refresh 避免 5200 次调用"一致))。
- `backtest/` 历史研究。`eval.py`（IC/IR/分档/因子计算）、`engine.py`（topN 回测，注意与 `screener/engine.py` 同名不同域）、`risk.py`（风控指标）、`robust.py`（walk-forward/bootstrap）、`candidates.py`（候选池排序）、`buffett.py`（巴菲特式基本面分析 v2：年报 EPS + 横截相对估值分位 + FCF 代理 + 杠杆校正 ROE + 负面预筛，**仅个股**，ETF 不适用；FCF 优先用 `fundamentals_cache` source=cashflow 算真实 FCF（经营现金流-资本开支），失败降级摘要代理（经营现金流量净额），`ratios.fcf_source` 字段标注来源；附带修复 `_annual` 兼容带破折号报告期 2024-12-31）、`signals.py`（买卖信号扫描：ma_breakout/golden_cross/volume_surge/rsi_oversold/momentum_up，依赖 `*_daily` 历史表，复用 `data.history._UNIVERSE`；`scan_signals` 支持 `min_hits` 共振过滤(每行带 `hits`/`combo`)，`backtest_signals` 支持 `min_hits`(≥2 追加 combo≥N 行)/`stop_loss`(前视 k 日 low 截断)/`fee_bps`(双边费率)；`_uni_panels` 加 `with_ohlc` 取 high/low，默认 2-tuple 回退兼容旧测试 mock）、`quality.py`（多口径共振编排层：四口径分位[风险调整/价值质量/资金流向/多信号]→共振分 hits×10+avg→min_dims 门槛→组合层[行业分散/相关性/容量]；**只读** buffett/signals/smart_money/candidates 结果，不新增表，个股/ETF 不混跑，ETF 口径2恒空；盘口精排阶段(2026-08-13)：四口径共振粗筛 top50 → pytdx get_quote 取实时盘口 → A 流动性深度重排(0.6共振+0.4流动性横截分位) + B 挂单不对称/C 内外盘比 仅 raw 展示不进排序(合规:方向=择时非质量),盘后自动降级(A/B失效仅留C全天内外盘),refine/refine_pool 参数,盘中缓存30s;ETF 与 refine=False 跳过。措辞"盘口供求机械观察",disclaimer 追加 A/B 收盘失效。**buffett 超时优化(2026-08-14)**：`analyze_many(codes, deadline_s=None)` 默认 None 走旧 `ex.map` 等全部完成；quality 口径2 传 `deadline_s=40.0`——akshare 财报接口慢(每只~15-20s 逼近 `_AK_TIMEOUT=20`)，80只 shortlist × 15-20s / 8 worker ≈ 150-200s，旧 `ex.map` 干等导致 /api/quality 首次 cache-miss 挂起~86s + 前端裸 fetch 无超时报"加载失败"。改 `as_completed` + 总截止返**已完成部分分位**(放弃未完成，`shutdown(wait=False, cancel_futures=True)` 不阻塞等残余线程≤20s自亡)，quality 凭口径1/3/4+部分口径2 仍产有效主清单；首次 86s→~50s(<前端75s AbortController 超时，用户首次即见结果)，5min 内重算走结果缓存秒回、5min-7天走 fundamentals_cache 7天缓存秒回。改 deadline 阈值同步 `tests/test_buffett_value.py` 的 `test_analyze_many_deadline_returns_partial`）。**熔断+预热(2026-08-15)**：① `buffett._note_fetch(ok)`/`akshare_blocked()` 运行时熔断——`fetch_abstract` 所有结果(成功/超时/异常/**空结果**)经 `_note_fetch`,**空结果亦计失败**(akshare 被封常快速返空 DataFrame 非 20s 超时,旧实现空结果走 fallthrough 不调 `_note_fetch` 致熔断永不触发);连续≥3失败→`_BLOCKED_UNTIL` 熔断30min,quality 口径2 检 `akshare_blocked()` 跳 `analyze_many` 省40s白烧→下方 spot估值代理接管。akshare 慢但成功时熔断休眠(fetch成功归零),仅被封(返空/错)时触发。改 `fetch_abstract` 同步 tests(空结果/成功重置2测试)。② `server.py` `_startup_warm`/`_warm_quality_cache_background` 启动+refresh 后后台 daemon 预热(非阻塞 `_warm_lock` 防叠加):`shortlist_by_turnover(k=80)` → `analyze_many(deadline_s=120)` 填 `fundamentals_cache` 7天 + `quality_rank`(默认参数对齐前端 qLoad 下拉)填结果缓存5min;`_AK_OK=False` 跳过;失败不影响 refresh/启动。冷启动后用户开 tab 命中缓存秒回。同步 `tests/test_quality_warmup.py`(5测试)。③ 前端 `qsLoad._qsFetch` 首次75s超时**自动重试一次**(90s):容器刚重启 warmup(~55s)与首次请求争抢可能>75s,重试时 warmup 多半已完→缓存命中;二次仍败才报"加载失败"。**因子体系升级(2026-08-15,Phase1+2)**：①`_avg_rank_pct(factors,codes)` rank-based 稳健聚合(每因子先 `_to_pct` rank-pct 再按 code 取可用因子 `mean(skipna)`,抗异常值+异质缺失)替 zscore 等权;②口径2 从 3 因子(ey/moat/lroe)扩到 6(+roic/mos/oet,全在 buffett.analyze 结果里,零新调用);③`screener/smart_money._behavior_batch(codes,days)` 批量行为序列(一次查 `smart_money_action since days 前`,按 code×channel 聚合 streak_inflow/outflow/margin_accel(近5日均-近20日均)/北向cum,复用 `_streak`,**不调 finshare 回退**——批量场景缺记录→对应字段 None),口径3 从 high-corr 三因子(top_by_amount累计+spot当日净额+换手)改为 low-corr 三因子(资金流连续性 streak_inflow+北向累计+边际加速)限 shortlist(非 shortlist 口径3=None,与口径2 一致),全空→降级旧 spot 路径(ETF/无行为序列)。改口径2/3 同步 `tests/test_quality_factors.py`(Phase1+2 共6测试)。**Phase3 共振层(2026-08-15)**：`_resonance(dim_scores,dim_thresh,weights=None,mode="greedy")` 加 mode + 默认经验权重 `_DEFAULT_DIM_WEIGHTS={1:1.0,2:1.3,3:0.6,4:0.7,5:1.3}`(因子有效性先验,非IC校准,口径2价值质量/5景气>1风险>4信号>3资金流);`mode="greedy"`(默认,hits×10+命中口径加权均值,数量偏好,共识优先)/`"penalize"`(命中口径**加权几何均值** `exp(Σwi·log(pci)/Σwi)`,pct裁剪[0.01,1]防log0,**短板惩罚**——某口径极低拉低总分;greedy 只对≥thresh 命中口径取均值,penalize 对所有 present 非None 口径取几何均值以真正惩罚短板;hits 独立返回供 min_dims 门槛);`dim_thresh` 默认 0.6→**0.7**(提区分度,主清单仍≥min_dims);`quality_rank` 加 `resonance_mode` 参数+缓存键含之;`/api/quality` 透传 `resonance_mode`(Query 默认 greedy)+ `dim_thresh` Query 默认 0.7;前端 `qsResMode` 下拉(贪心共识/短板惩罚)+ `qsThresh` 默认 0.7+ onchange 触发 qsLoad;warmup `dim_thresh=0.7,resonance_mode="greedy"` 对齐前端默认缓存键。改共振同步 `tests/test_quality_factors.py`(Phase3 共4测试:penalize惩罚/greedy不变/默认阈值0.7/默认权重)+修 `tests/test_quality.py::test_resonance_hits_formula` 显式传 `weights={}` 测纯等权公式(默认权重改非等权)。Phase4 口径5景气/IC 透明度报告待续。
- `api/server.py` FastAPI 单文件入口，所有路由 + `_wrap`。`web/index.html` 单页前端（原生 JS，无构建步骤，从 `/api/fields` 动态渲染字段/运算符下拉）。5 tab 分组（实时筛选/历史回测/主力动向/优质筛选/个股分析）+ 持仓右上浮窗抽屉（`#pfDrawer`，`togglePortfolio()`）+ 自选观察抽屉（`#wlDrawer`，`toggleWatchlist()`，`wlLoad/wlAdd/wlQuickAdd`）+ K线跨 tab 跳转（行点击 handler 调 `switchTab('backtest')` 再 `hkRun(c)`；任意股票代码行点击调 `switchTab('analysis')` 并填码跑 `/api/stock-analysis`）；信号扫描行与个股分析卡带"＋自选"按钮调 `wlQuickAdd(code)` 一键入自选；`.tab-panel[data-tab]` 显隐，状态保持不重新 fetch。

### 路由速查（server.py 全量）

`/api/meta`（最近一次 refresh 的 `update_time`，无数据时为空）`/api/health`（数据健康聚合只读：各域新鲜度三态红/黄/绿 + overall，默认 disclaimer）`/api/market`（市场温度快照+近30日趋势，只读，默认 disclaimer："机械市场状态观察，非择时信号"）`/api/fields` `/api/boards` `/api/etfs` `/api/board-stocks`（板块详情卡+成分股列表：优先东财 `stock_board_industry_cons_em`/`concept_cons_em` 直取(板名同源最准)，东财被封/空→降级 THS 静态表+模糊匹配，两源失败→龙头股无成分股诚实标 `cons_error`，`source` 字段从首行派生，默认 disclaimer）`/api/screen`（实时筛选，附 `disclaimer`）→ `/api/refresh`（手动全量采集，`@app.api_route` GET+POST 均可）→ `/api/history`（历史日线查询）→ `/api/backtest/fetch`（拉历史进 `*_daily` 表）`/api/backtest/eval`（IC/分档）`/api/backtest/run`（topN 回测）`/api/backtest/walkforward`（附 `bt_disclaimer`）→ `/api/candidates`（候选池排序，附 `cand_disclaimer`）→ `/api/signals`（机械信号扫描，支持 `min_hits` 共振过滤，附 `cand_disclaimer`："规则机械触发，非AI推荐，不构成投资建议，盈亏自负"）`/api/signals/backtest`（信号历史胜率回测：每信号扫历史触发点 t→t+k 收益统计；支持 `min_hits`(≥2 追加 combo 共振行)/`stop_loss`(前视止损)/`fee_bps`(双边费率)；前端"信号回测研究台"卡多 k 并行渲染 signal×k 矩阵，附 `cand_disclaimer`："历史触发统计事实，非预测"）→ `/api/portfolio` GET/POST + `DELETE /api/portfolio/{pid}`（本地持仓跟踪：GET/POST 用默认 `_wrap` 附 `disclaimer`，DELETE 按 id 平仓返回 `{closed,id}`，无额外 disclaimer）`PATCH /api/portfolio/{pid}`（到价提醒 `alert_hi`/`alert_lo`，返回 `{updated,id}`）+ `/api/watchlist` GET/POST + `DELETE /api/watchlist/{wid}`（自选股观察清单：同 code 重覆更新；默认 `_wrap` disclaimer；前端抽屉 `#wlDrawer` + 信号/分析"＋自选"联动）→ `/api/buffett` `/api/buffett/top`（基本面评分，附 `bt_disclaimer`："基于公开财务摘要的机械评分，护城河来源需读年报人工判断，研究优先级非买卖信号"）→ `/api/smart-money/today` `/api/smart-money/refresh` `/api/smart-money/channels`（主力动向观察清单，`today`/`refresh` 附 `cand_disclaimer`："主力动向观察清单，机械归类，非荐股非买卖信号"；`channels` 返回 `smart_money.channel_status()` 各通道 ok/err/rows/date，叠加 DB 实况——最新日期有行即 ok=True，防容器重启后内存态 `CHANNEL_STATUS` 全归零「未采集」明明有数据却全显灰）`/api/smart-money/seats`（游资营业部龙虎榜统计，席位级按需 on-demand：上榜次数/买入额/卖出额/净额，period∈近一月/近三月/近六月/近一年，附 `cand_disclaimer`）`/api/smart-money/seats-stocks`（游资追逐个股，席位明细按需 on-demand：对该日龙虎榜个股逐个取买入席位，慢 N股×1调用，仅个股 ETF不上龙虎榜，附 `cand_disclaimer`）→ `/api/smart-money/behavior`（主力行为时间序列：连续净流入/流出天数·累计净额·边际加速·多通道 daily，只读 `smart_money_action` 近 N 日，附 `cand_disclaimer`："主力行为序列机械统计，非买卖信号，盈亏自负"）→ `/api/chip`（个股筹码分布：移动成交量加权成本 avg_cost/获利盘·套牢盘比例/chip_concentration/chip_range_90/分箱直方图，依赖 `stock_daily` 历史(需先 `/api/backtest/fetch` 拉该 code)，无历史→`need_history=True`，**不入库不触网**进程内算，附 `cand_disclaimer`："移动成本分布机械统计，非支撑/压力位预测，非买卖信号，盈亏自负"）→ `/api/tdx/quote`（单股实时五档行情：通达信 TCP 7709 直取，盘中实时免 key，`get_quote` 返 price/last_close/open/high/low/vol/amount/bid1-5/ask1-5，附 `cand_disclaimer`："实时行情机械快照，非买卖信号"）`/api/tdx/company-info?code=&category=`（通达信公司信息文本块按需取：16 类[龙虎榜单/主力追踪/股东研究/财务分析/股本结构/研究报告 等]，**第一版返原始文本不解析**，前端 pre 预格式显示；注意通达信"龙虎榜单"类别实含【融资融券/资金流向/异动/大宗交易】非游资席位龙虎榜，附 `cand_disclaimer`）→ `/api/stock-search`（股票代码/名称模糊搜索，查 `stock_spot` code LIKE OR name LIKE，供前端输入框自动补全，机械查询非荐股）→ `/api/st-list`（ST 全名单快照，默认 disclaimer）→ `/api/management`（高管增减持，channel=高管增减持，附 `cand_disclaimer`）→ `/api/share-unlock`（限售解禁按 as_of 月份查，附 `cand_disclaimer`）→ `/api/research`（研报评级，近 N 日，附 `cand_disclaimer`："机构研报评级机械汇总，非荐股非买卖信号"）→ `/api/comments`（千股千评 per-code 按需+缓存 source=comments，附 `cand_disclaimer`）→ `/api/quality`（多口径共振优质筛选，附 `cand_disclaimer`："多口径共振机械排序观察清单，非荐股非买卖信号"；复用 buffett/signals/smart_money/candidates 因子，不新增表；依赖 `*_daily` 历史的口径[风险调整/多信号]未 fetch 则空并降级 `min_dims`）→ `/api/stock-analysis?code=`（个股深度分析卡：**inline 聚合在 server.py，无独立模块**——聚合 buffett 基本面+评分(0-100 机械：ROE/毛利率/负债/FCF/护城河/估值-红旗)+主力动向(近30日各通道净额/每日 sparkline)+研报评级+千股千评+技术信号(scan_signals)+多因子机械预判 `outlook`：**连续加权版**——`WEIGHTS`(基本面0.30/资金面0.20/技术面0.20/机构评级0.15/估值0.10/内部人0.05)×连续 `strength∈[-1,1]`→`score`∈[-1,1]，`score>0.15`偏多/`<-0.15`偏空/else中性，`confidence`=|score|×100；`reasons` 每条带 `weight`/`strength`，`contribs` 记每因子加权贡献；附 `bt_disclaimer`："基于公开数据的多维机械分析+多因子预判,研究优先级非买卖信号,盈亏自负"）。新增路由时按所属域挂对应 disclaimer。根路由 `/` 返回启动信息 + `/docs`/`/web` 指针 + `disclaimer`；`/web` 静态托管前端（同源，免 CORS）。

> 交互调试：FastAPI 默认 Swagger UI 在 `/docs`（ReDoc 在 `/redoc`），可手工调任意路由、看 schema 与 `disclaimer` 字段。

### 关键设计决策（非显而易见，改前必读）

**AKShare 数据源在当前出口 IP 被东财封**（`RemoteDisconnected`/502）。代码已加备援，改动时勿破坏：

- `data/collector.py` 顶部 `_install_http_patch()` 全局给 eastmoney 域名注入浏览器 UA + Referer，并对 `RemoteDisconnected`/502/503/504 退避重试 4 次。**必须保留**——akshare 的板块接口 `requests.get(url, params=params)` 不带 headers，默认 `python-requests/*` UA 被东财 502。
- 板块采集：东财 `stock_board_industry_name_em` 失败 → `stock_board_industry_summary_ths`（同花顺，字段：板块/涨跌幅/净流入/上涨家数/下跌家数/领涨股/领涨股涨跌幅）。概念：东财失败 → `stock_board_concept_summary_ths`（仅龙头股/成分股数量/驱动事件，**无涨跌幅/资金流**——这是已知限制）。
- 资金流 `ak.stock_sector_fund_flow_rank`：`sector_type` 合法值是 `行业资金流/概念资金流/地域资金流`（**不是** `行业/概念`），`indicator` 只支持 `今日/5日/10日`（**无 20日**）。`collector._SECTOR_TYPE_MAP` 做了简称映射，对外仍接受 `行业/概念`。
- 历史日线：**通达信主源**(`_fetch_hist_tdx` 取 raw → `adjust.qfq` 本地算前复权,标 `source=tdx主源,本地qfq`)，akshare 新浪 qfq(`fund_etf_hist_sina`/`stock_zh_a_daily`)降为备援(东财历史接口全被封,新浪偶可用)；板块走 `stock_board_industry_index_ths`（best-effort，THS symbol 映射可能不全）、基准 `stock_zh_index_daily`。**pytdx `fenhong`/`songzhuangu`/`peigu` 为"每 10 股"单位(非每股)**——`adjust.get_xdxr` 归一化时 ÷10,`qfq` 直接用每股值(勿在 `qfq` 里再 ÷10,否则过度复权导致除权日跳变)。`qfq` 算法:先用 raw 收盘算全部 ratio(避累乘污染前收)→再按除权日降序把 ratio×到对应位置之前的所有价格、volume ÷扩股比。

**NaN→None 序列化**：`screener/engine.py` 两处与 `collector._to_records` 必须用 `df.astype(object).where(pd.notna(df), None)`。**不能**用 `df.where(pd.notna(df), None)`——对 float64 列 None→NaN 又变回 NaN，starlette 的 JSONResponse `allow_nan=False` 会直接 500（`Out of range float values`）。

**日期范围过滤**：`data/history._filter_range` 与 `backtest/eval.load_panel` 用 `pd.to_datetime` 比较，**不能**用字符串比较——数据日期是 `2024-06-01` 带破折号、API 参数是 `20240601` 无破折号，字符串比较会让 2023 全排除/2024 全纳入。

**DB 迁移**：`db.init_db` 先 `executescript(SCHEMA_SQL)`（`CREATE TABLE IF NOT EXISTS` 不会给已存在的表补列），再 `_migrate` 对旧库 `ALTER TABLE ADD COLUMN`（try/except 忽略 duplicate column）。新增列时同时扩 `_BOARD_MIGRATIONS`（或类比的新表迁移）——旧库在持久化卷里，否则字段缺失。

**pandas 3.0 兼容**：`pd.Grouper(freq="M")` 已弃用→用 `ME`/`QE`（`backtest/engine.rebalance_dates` 有 `_map` 归一）；`Series.reindex` 的 fill_value 必须用关键字（`b.reindex(comb, fill_value=0)`，不能位置参数）。

**回测因子可历史重建的只有 OHLCV 派生**（`momentum_n/volatility_n/turnover_n/activity/momentum`）。spot 快照因子（`up_count/down_count/main_net_inflow/leading_stock_change`）是当日盘后聚合、无历史序列，**无法回测**——改 `BACKTEST_FACTORS` 时勿把 spot 因子塞进去。

**signals/portfolio/buffett 的数据依赖链**：
- `signals.scan` 依赖 `*_daily` 历史表（个股 `stock_daily` 等），**必须先 `/api/backtest/fetch` 拉过对应 codes 的历史**，否则空表无信号。RSI/MA 全部在 `signals.py` 本地重算，不入库。
- `buffett.fetch_abstract` 走 `ak.stock_financial_abstract`（个股财务摘要），是**按需实时拉取**、不入库——`_AK_OK=False` 时整个 buffett 路由返回 None，不要假设它有缓存兜底。`/api/buffett/top` 先从候选池 shortlist（依赖 `/api/refresh` 的 spot 快照排除 ST/涨停/停牌）取标的，再逐个拉财务摘要评分。
- `portfolio` 是独立的 `portfolio` 表（`id/code/name/buy_date/buy_price/shares/note/ts`），与行情表解耦；浮盈靠 `stock_spot` 最新价现算，无 spot 时浮盈字段为空。
- **`smart_money_action.date` 列混有未来日期**（限售解禁通道把未来解禁日 `as_of` 也写进 `date`）。`screener/smart_money.today_list` 省略 date 时取"最新日期"**必须封顶 today**（`where="date <= ?"` today），否则 `order_by date DESC` 取到未来解禁日（如 2026-08-31），`date BETWEEN start AND 未来` 把今日真实资金流/龙虎榜全过滤掉，today 视图只剩未来解禁行——即"深查主力没效果"的根因。改 `today_list` 默认窗口逻辑时勿去掉这层封顶。

## 改动检查清单

> **设计文档**：`docs/superpowers/specs/` 存 12 份设计 spec、`docs/superpowers/plans/` 存对应 10 份实施 plan（`backtest-realism` 与 `chip-distribution` 仅有 spec 无 plan）（smart-money-tracker / quality-screener / frontend-tabs / akshare-more-sources / backtest-factors / signals-etf-combo / backtest-realism / data-health-dashboard / sm-quality-reliability / chip-distribution / quality-quote-refine / watchlist-live-monitor）。改某模块前先读对应 spec，拿到"为什么这么设计"的上下文比读代码回溯快。

- 新增 SQLite 列/表 → 同步 `models.SCHEMA_SQL` + `TABLE_FIELDS` + `db._BOARD_MIGRATIONS`（若旧表补列）。
- 新增采集源 → 复用 `(df, ok, err)` 返回约定 + `_to_records`（NaN→None）+ 异常不抛崩；eastmoney 域名自动受 HTTP patch 保护。
- 新增 AKShare 采集源 → 列表型(stock_zh_a_st_em/stock_hold_management_detail_em/stock_restricted_release_detail_em/stock_research_report_em)进 refresh_all/refresh_today;per-code(stock_*_sheet_by_report_em/stock_comment_detail)走 fundamentals.fetch + fundamentals_cache 缓存(7天 TTL,超时降级 stale);复用 `_first_col` 候选名容错,单测 mock 验证字段映射。高管增持仓/限售解禁并入 smart_money_action 复用 channel 零加列;限售解禁 as_of=真实解禁日期(可能未来),查询按 as_of 月份(unlock_by_month)。
- 新增 finshare 备援源 → `data/smart_money.py` 的 `collect_dragon_tiger` 东财主源(`stock_lhb_detail_em`)空或异常(被封)→ finshare `get_lhb()` 备援(不走东财 push2 被封端点,实测可用;净买/买/卖额拆分比东财主榜单更全)。finshare 函数内 `import`(独立于 akshare,`_AK_OK=False` 也走备援);多日榜单按 `trade_date`(无破折号 YYYYMMDD)经 `_norm_date` 过滤当日;`fs_code`('000004.SZ'/'SH600519'/'sz000004')经 `_fs_code_norm` 归一 6 位纯代码;finshare lhb **无 name 列**→记录 name 留空,下游 `stock_spot` 按 code 补;两边皆无当日上榜→成功空(不算错),两边败→False 诚实标 `source=finshare`。**finshare 资金流接口(`get_money_flow`/industry/concept)同源东财 push2 被封→不可作资金流备援**(THS 直取仍是唯一可靠全市场源);finshare 2.1.0 已移除北向函数→北向需另选源(a-stock-data 官方交易所备胎)。但 `get_money_flow_stock(code)` 个股历史主力净额序列**不走东财被封端点(实测可用)**→`screener/smart_money.behavior_series` 资金流通道表无记录时按需补(标 `source=finshare`,截近 days 日,仅个股分析卡/behavior 按需调,不进 refresh 全量避免 5200 次调用)。`requirements.txt` 加 `finshare>=2.0.0`。
- 新增 pytdx 直连源(通达信行情服务器,免 key TCP 7709,不走 akshare) → `data/pytdx_client.py` 薄客户端:服务器池轮询+单例复用(`_get_api` 带 `threading.Lock` + 心跳重连,FastAPI 多线程安全)+`_market`(5/6/9→沪=1,0/1/3→深=0[1 开头=深市 ETF],4/8→北交=2 未实测失败降级)+`_project_symbol`(复用 history._sina_symbol 规则,保证落 stock_daily.symbol 列一致)。`get_quote(codes)` 批量五档实时(≤80/批分页,NaN→None 复用 `_nan`);`get_daily_bars(code,count)` 日 K(**不复权**,`_BATCH=800` 偏移分页累加,列映射 vol→volume/datetime→date 截 YYYY-MM-DD,drop_duplicates+sort);`get_company_info(code,category)` 公司信息文本块(16 类,**第一版不解析返原文本**,通达信"龙虎榜单"类别实含【融资融券/资金流向/异动/大宗交易】**非游资席位龙虎榜**,后者走 finshare/`/api/smart-money/seats`)。**不进 refresh_all**(TCP 按需同 history)。**history.fetch_stock_hist/fetch_etf_hist 以 tdx 为主源**:`_fetch_hist_tdx` 取 raw → `adjust.qfq` 本地算前复权(标 `source=tdx主源,本地qfq`),akshare 新浪 qfq 降为备援(tdx 空/失败/连不上时);`get_daily_bars` 旁加 `get_xdxr` 包装取除权除息因子。**pytdx xdxr `fenhong`/`songzhuangu`/`peigu` 为"每 10 股"单位**,`adjust.get_xdxr` ÷10 归一化为每股,`qfq` 直接用每股值(勿再 ÷10,否则过度复权、除权日出现 4.x→6.x 的跳变)。复权失败(无 xdxr/异常)退化为不复权,诚实标注。tdx 单例连接受网络抖动影响,长 count(多批分页)偶发连不上→上层自动降级 akshare 备援;portfolio.list_positions spot 缺价→pytdx 兜底 latest_price(单测需 mock `get_quote` 避免真连网);stock_analysis inline 加 `latest_price`/`quote` 字段。路由 `/api/tdx/quote`(单股实时五档)+`/api/tdx/company-info?code=&category=`(按需文本),挂 `cand_disclaimer`:"通达信行情/公司信息机械汇总,非买卖信号,盈亏自负"。`requirements.txt` 加 `pytdx>=1.70`。
- 板块成分股(`data/board_stocks.py`) → **按需实时拉取不入库**(区别于 refresh_all 列表型源)；`fetch_constituents(board,category)` **优先东财直取**(`stock_board_industry_cons_em`/`stock_board_concept_cons_em`，`symbol=板名` 直接取无需 name→code 映射，板名来自东财 `stock_board_industry_name_em` 时同源最准)，东财被封/akshare 未装/返回空→降级 THS 静态表(`_fetch_map` 建 name→code 映射 + `_fuzzy_match` 包含/被包含取最长命中，兜底近似板名)；两源都失败→抛 ValueError 由路由 catch 降级(返回龙头股无成分股，诚实标 `cons_error`)；每行带 `source`(em|ths)，路由 `source` 字段从首行派生不再硬编码。东财路径调 `collector._install_http_patch()` 注入 UA/Referer+重试(否则默认 UA 被 502)；翻页至空或行数<20 判末页；read_html 把代码列读成 int 丢前导零(002966→2966)→**必须 `zfill(6)` 补回**(两源同)；`_prefix` 加交易所前缀(6→sh/0/30→sz/920/43/83/87/8→bj)；NaN→None(复用 `_f`/`_s`，防 `allow_nan=False` 500)。**`pd.read_html` 必须显式 `flavor='lxml'`**——pandas 3.0 默认 flavor 走 bs4→html5lib，html5lib 未装即 `ImportError`(板块成分股报错降级、`data/smart_money.py` THS 资金流 `_rows` 静默返回 `[]` 资金流通道空)；`lxml` 已 pin `requirements.txt`。同坑见 `smart_money.py` 的 `_rows`(两处都要 flavor='lxml')。

  > **AKShare 版本适配（2026-07-23 修正，akshare 1.18.64）**：旧函数大面积改名，按旧名调用直接 `AttributeError` 导致 5 通道零写入（DB 只剩资金流）。映射：龙虎榜 `stock_lhb_detail_em(start_date,end_date)` 日期须**无破折号 YYYYMMDD**（带破折号内部 NoneType 崩）；席位明细 `stock_lhb_stock_detail_em(symbol,date,flag)` 现需 date+flag、逐股两次请求太慢，**改用主榜单"龙虎榜净买额"出个股级记录**。十大股东 `stock_gdfx_free_top_10`→`stock_gdfx_free_top_10_em(symbol=sh/sz+code, date=报告期YYYYMMDD)`（`_prefix_code`/`_latest_report_period`）。高管 `stock_hold_management_em`→`stock_hold_management_detail_em()`（无参、17 万行全历史、拉取约 4-5 分钟，`_filter_recent` 按"日期"列取近 7 日，记录 date 取行内变动日期）。限售 `stock_share_change_em(symbol=月)` 已下线→`stock_restricted_release_detail_em(start_date,end_date)` 按日期范围拉个股清单（actor 取"限售股类型"，无"解禁股东"列）。北向两东财端点均 `NoneType` 崩（反爬），无备援，保持优雅失败灰显。`_clean` 已支持 date/Timestamp→字符串（raw 列含上榜日/解禁时间等 date 对象，否则 JSON 序列化 500）。
  >
  > **资金流通道 THS 直取（2026-07-23）**：原口径复用 `stock_spot.main_net_inflow`，但该字段来自东财个股资金流（`stock_individual_fund_flow_rank` 出口 IP 被封 RemoteDisconnected），新浪 `stock_zh_a_spot` 又无该列 → 资金流通道长期停摆。`collect_fund_flow` 改走**同花顺直取**：`_fetch_ths_individual_fund_flow` 绕过 akshare `stock_fund_flow_individual` 的列名 bug（akshare 硬编 10 列、THS 即时表实际列数已变致 `ValueError: Length mismatch`），直接取 `data.10jqka.com.cn/funds/ggzjl/...` ajax 分页，`hexin-v` token 复用 akshare 内部 `_get_file_content_ths('ths.js')`+`py_mini_racer`（模块级缓存 TTL10min,失效重算;分页 `ThreadPool` 8 并发,~70s→5-8s），`read_html` 按表头取 `股票代码`/`股票简称`/`净额(元)`，`_parse_cn_amount` 解析"822.74万"/"1.63亿"/负数。THS 失败再回落 spot.main_net_inflow（东财残量，通常空）。THS 不封东财 IP，是资金流通道唯一可靠源（~5200 行/当日，拉取约 1 分钟）。
- 新增 API 路由 → 用 `_wrap()`，回测/候选池类附对应 disclaimer。
- 新增 quality 编排层 → **不新增表**（复用 stock_spot/etf_spot/*_daily/smart_money_action），因子源文件 buffett/signals/smart_money/candidates **只读结果不改**；口径分位缺失为 None（`hits` 不计），任一因子源失败标 `dim_status=err` 不崩；`*_daily` 未 fetch 时口径1/4 空并 `min_dims` 自动 clamp。盘口精排阶段(pytdx get_quote)守同约束:仅 universe=stock+refine+小名单(top refine_pool=50)触网,非全市场;A 流动性深度进综合分(0.6共振+0.4流动性),B/C 方向不进排序仅 raw(合规);盘后 in_session=False 自动降级 A/B=None 仅 C;_is_in_session 注入 now 可测;改权重/阈值同步 tests/test_quality_refine.py(15 测试)。
- 改 `/api/stock-analysis` 个股分析卡 → **逻辑 inline 在 `server.py`，无独立模块**（区别于 buffett/signals/quality 等领域模块）；聚合顺序：buffett.analyze→评分(0-100 机械)→`db.query_rows("smart_money_action")` 近30日→research 研报+千股千评→`scan_signals`→多因子预判 `outlook`。改预判因子权重/阈值直接改 server.py 内 `stock_analysis()` 的 `WEIGHTS` 与 `_push` 闭包；每段 try/except 独立兜底，单源失败不拖垮整卡。措辞保持"多维机械分析/研究优先级非买卖信号"，挂 `bt_disclaimer`。
  > **outlook 多因子预判连续化（2026-08-13）**：`_push(factor,d,detail,strength=None)` 用连续 `strength∈[-1,1]` 替原 ±1/0 三态（`None` 时按 dir 退化为 ±1/0，触发型因子兼容）；`contrib=weight×strength`，`score=Σcontrib∈[-1,1]`，`label` 阈值 ±0.15 不变，`confidence=min(|score|,1)×100`。六因子 `WEIGHTS`={基本面评分0.30/资金面0.20/技术面0.20/机构评级0.15/估值0.10/内部人0.05}，**合计=1.0**（基本面从 0.35 挪 0.05 给内部人，权重为经验先验未经回归校准）。各因子 strength 来源：基本面 `(card.score-50)/50`（score 由 step1 从 ratios 机械算，非 ba.score）；估值 `tanh(margin_of_safety×3)`（MoS 来自 buffett，无 MoS 则回退 valuation_tag 三态）；资金面调 `sm_query.behavior_series(code,30)` 用 `streak_inflow`/`streak_outflow`（`streak≥5` 才触发，强度 `tanh(streak/8)`，**替原 raw 净额合计**——单日大额不再误判偏多）；技术面/机构评级命中数 `min(count/3,1)`；**内部人因子**查 `smart_money_action where channel='高管增减持' AND code=? AND date>=近30日` 净额方向（注意 channel 值在 `params` 里不在 `where` 串里，mock 时按 params 判别）。改 outlook 因子/权重时同步 `tests/test_outlook_continuous.py`（8 测试 TestClient+全 mock，`_RATIOS_HIGH/LOW` 控制 card.score）。
- 新增筹码分布/主力行为序列 → `screener/smart_money.py` 的 `chip_distribution`/`behavior_series` **不新增表、不触网**：chip 复用 `backtest.signals._uni_panels` 取 `stock_daily` close+amount 进程内算移动成交量加权成本(收盘价代理成交价)，依赖 `*_daily` 历史(需先 `/api/backtest/fetch`)，无历史→`need_history=True`；spot 取值优先级 param>`stock_spot.latest_price`>daily 末值；NaN→None 复用 `_nan`/`_weighted_percentile`/`_histogram`(防 `allow_nan=False` 500)；`behavior_series` 只读 `smart_money_action` 按 channel(资金流/北向/龙虎榜) 聚合到 code-date 级算连续性/边际(`_streak` 算连续末尾正负天数)。措辞"机械统计/非支撑压力位预测/非买卖信号"，挂 `cand_disclaimer`。
- 自选股(watchlist) → 镜像 portfolio：`data/watchlist.py` 独立 INSERT(不走 `upsert_rows`/`TABLE_FIELDS`)；同 code 重覆加 → UPDATE 不重复插入；`list_items` 按 `id DESC`（不按 `added_ts`，避免同秒并列乱序）；表只加 `CREATE TABLE IF NOT EXISTS watchlist` 进 `SCHEMA_SQL`，**无需进 `_BOARD_MIGRATIONS`**（新表 IF NOT EXISTS 启动即建，迁移只给旧表补列）。前端"＋自选"按钮统一调 `wlQuickAdd(code)`（信号扫描行带 `event.stopPropagation()` 防触发行跳 analysis）。
- 新增前端字段/运算符 → 下拉已从 `/api/fields` 动态渲染，通常无需改 JS；但 `LABEL` 字典要补中文标签。
- 新增 signals/buffett 信号规则 → signals 规则在 `signals.py` 内本地重算（不入库），buffett 评分维度改 `buffett.py` 的 v2 逻辑；两者措辞保持"机械评分/研究优先级/非买卖信号"，按域挂 `cand_disclaimer`/`bt_disclaimer`。signals 已支持 `min_hits`/`stop_loss`/`fee_bps` 研究参数（共振过滤、前视止损、双边费率），改 `_uni_panels` 时须保持默认 `with_ohlc=False` 返回 2-tuple（旧测试 mock 兼容），`with_ohlc=True` 返 4-tuple；前端"信号回测研究台"卡多 k 并行调 `/api/signals/backtest` 渲染 signal×k 矩阵。
- 改 buffett 价值评估口径（**零新数据源**，全用 `fundamentals.fetch(code, balance/cashflow/profit)` 已能取到的完整三大表 + 摘要序列）→ `buffett.analyze` 现含：**所有者收益**（巴菲特 1986：`ni+折旧摊销-资本开支-营运资本增加`，`ratios.owner_earnings`/`owner_earnings_to_ni`，`<0.5` 标红旗"赚非现金"；科目缺失降级现有 FCF 代理，`fcf_source` 标注不变）；**杜邦分解**（`net_margin×asset_turn×equity_mult≈报告ROE`，偏差>3pt 标 `dupont_note`，`equity_mult>3` 标"ROE靠杠杆"）；**ROIC**（`NOPAT=ni+利息×(1-tax_rate)`，`invested_capital=总资产-无息流动负债`简化口径，`>15`高资本回报）；**增长持久性**（`_cagr` 算 rev/eps/bps/ni 多年 CAGR，n≥3 才算；`_consistent_years` 数连续 ROE>15 年数；**`_cagr`/`_pick_row_fields`/`_pick_col_sum` 假设输入序列降序(新→旧)**，mock 必须降序喂）；**轻量 DCF（Gordon-on-Book 剩余收益）**：`g=观测BPS CAGR`（截 `[0,roe_avg/100]`，零新数据源），`r=_COST_OF_EQUITY=0.09`，`justified_pb=(sustainable_roe-g)/(r-g)`，`IV=justified_pb×bps_latest`，`MoS=(IV-price)/IV`；**`g≥r` 模型失效**→`justified_pb=None`+`dcf_note`标失效（两分支都设 `dcf_assumptions` 透明披露假设）；`valuation_tag` 整合 MoS（`>0.3`便宜/`[0,0.3]`合理/`<0`贵），与横截相对分位并存；`rank_top` 预筛加 `owner_earnings_to_ni<0.3` 排除 + `priority` 同档按 MoS 降序 + `_build_reasons` 加 IV/MoS/ROIC 叙事。措辞"安全边际/研究优先级/机械剩余收益估值"，**不用"低估买入/卖出"**，挂 `bt_disclaimer`："基于公开财务数据的机械剩余收益估值，假设 ROE/折现率/增长率，非真实内在价值，研究优先级非买卖信号，盈亏自负"。`_pick_row_fields`/`_pick_col_sum` 判年报须 `.replace("-","").endswith("1231")`（报告期带破折号"2024-12-31"，裸 `endswith("1231")` 尾 4 字符是"2-31"漏判）。改这些口径时同步 `tests/test_buffett_value.py`（15 测试 mock 三大表+摘要，bps 序列必须降序）。
- 单测放 `tests/`，合成数据 mock `db.query_rows`，不依赖网络。
- 勿提交噪声文件：`*.stackdump`（bash 崩溃转储）、`*.db`/`*.log`/`__pycache__/`（`*.stackdump` 与 `*.db`/`*.db-journal` 已在 `.gitignore`；仓库里曾误提交一个 `bash.exe.stackdump`，已删除）。
